{"date":"2026-08-31","technique":"post-landing-repair-density","subject":"delivery-analytics","bundle":"software-engineering","seam":"the absence of one: 0 reverts in 267 commits, so revert-linkage — the only risk signal readable from git alone — returns nothing here. The seam is scripts/, where a measuring script (docs-catchup.sh) already establishes the precedent for a maintainer-facing instrument that only measures.","mode":"code","verdict":"better","note":"Shipped as scripts/repair-density.sh (685da11, not pushed). Arm A = revert count: 0 at v0.0.7 and 0 at v0.0.4. Arm B = repairs per conventional-commit scope after the instant: 10 units at v0.0.7 (59 commits), 20+ at v0.0.4 (218 commits). Arm B produces a ranking where arm A produces nothing, which is the technique's claim. THE SEVERITY COLUMN WAS BUILT, MEASURED AND REMOVED, and that is the durable half: hardening and repair share a vocabulary, so keyword classing cannot separate 'fixed a panic' from 'added a panic guard' — 3 of its 4 top-ranked units were hardening commits, incl. security(supply-chain): secret scanning+dependabot scored as two security repairs. First number 6 alarming repairs, hand-verified 1. The shipped script prints repair SUBJECTS for the top units instead of classifying them; counts rank the reading order, a person ranks the risk. Blind spots printed in its own output: unit is the scope (proxy for a feature, splits and merges), and 4 of 59 in-range commits carry no scope. Proven red on all three refusal paths before trusted green. Tests: core 117 ok, manifest_guard 7 ok, gate_table_guard 2 ok; catchup_marker_guard red on .ai/registry-map.json missing from the agent marker — PRE-EXISTING from 4070e17, reproduced with the change stashed, untouched.","return_condition":"a second declared instant passes (next release tag) and the ranking is read against what actually needed fixing; also whether scope remains a usable feature proxy as the workspace grows"}
{"date":"2026-08-31","technique":"reclassification-is-not-repair","subject":"error-handling","bundle":"software-engineering","seam":"crates/api/src/error.rs — ErrorCode::Internal, constructed at exactly 3 sites outside the error module: events.rs:364 and state.rs:163 (task join errors) and otlp/mod.rs:158 (encoding the server's own batch response).","mode":"experiment","verdict":"not-better","note":"The fork arm ran statically over the tree; the detector arm could not. All 3 construction sites answer NO to the separating question (can a user reach this through a documented interface with values it accepts?), so declaration was right, caller is the bug, and there is nothing to reclassify. The tree had ALREADY taken the other branch deliberately: ErrorCode::Unsupported was split out of Internal with a comment saying a permanent capability gap must not read as a transient outage. Same reasoning again in the parsing direction at e7fcc7c, which replaced unwrap_or_default() in parse_enum and drew the fork per vocabulary — Provider::Unknown and Operation::Other are deliberate quarantine variants, status and redaction have no such member. not-better is correct and it amended the technique: a taxonomy already split deliberately gives the fork nothing to find. THE DETECTOR HALF IS GENUINELY ABSENT AND CHEAP — the only assertions naming the class are two unit tests of the error type itself; zero tests assert it never fires across the suite, and the expensive prerequisite (a stable wire string with a test pinning it) is already built.","return_condition":"build a suite-wide sweep of integration-test responses for the \"internal\" wire string and re-run; until that instrument exists the detector claim is asserted and unmeasured"}
