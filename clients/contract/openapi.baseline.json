{
  "components": {
    "schemas": {
      "AlertChannel": {
        "$defs": {
          "AlertKind": {
            "description": "What kind of condition fired. One variant per alert this system has ever produced — the wire\nliterals match the `event` field webhook receivers have always been switching on, so a receiver\nwritten before the ledger existed keeps working.",
            "oneOf": [
              {
                "const": "limit_breach",
                "description": "A limit rule breached (`LimitStatus.breached`).",
                "type": "string"
              },
              {
                "const": "limit_warning",
                "description": "A limit rule crossed its `warn_at` fraction without breaching.",
                "type": "string"
              },
              {
                "const": "forecast_alert",
                "description": "A pre-emptive forecast alert: budget breach ETA or margin erosion.",
                "type": "string"
              },
              {
                "const": "relay_task_dead",
                "description": "A relay task dead-lettered (attempts exhausted, or the device vanished).",
                "type": "string"
              },
              {
                "const": "error_spike",
                "description": "A burst of failed calls in one project inside the rolling error window.",
                "type": "string"
              },
              {
                "const": "score_drop",
                "description": "The recent mean verdict for one (project, rubric) regressed below its baseline.",
                "type": "string"
              },
              {
                "const": "bench_run",
                "description": "A benchmark run finished (the CI gate contract's completion hook).",
                "type": "string"
              },
              {
                "const": "ingest_rejected",
                "description": "A periodic flush of the in-process rejection ledger: how many ingest attempts a cap turned\naway. Deliberately an alert row and never an event — a rejected call was never stored as an\nevent precisely because it would corrupt the usage rollups every cap is evaluated against.",
                "type": "string"
              },
              {
                "const": "prompt_canary_regressed",
                "description": "The version a prompt label serves is measurably worse than the version it is being compared\nagainst (M23). The one alert that fires *after* a promotion, which is where a registry\npreviously stopped looking.",
                "type": "string"
              },
              {
                "const": "relay_task_unroutable",
                "description": "Queued relay tasks name an action no enrolled device advertises any more (M18): the fleet\nchanged under work that was routable when it was accepted.",
                "type": "string"
              }
            ]
          },
          "ChannelKind": {
            "description": "Where an alert goes.",
            "oneOf": [
              {
                "const": "webhook",
                "description": "A JSON POST, signed (see `X-LightTrack-Signature` in `docs/ALERTS.md`).",
                "type": "string"
              },
              {
                "const": "ntfy",
                "description": "A plain-text POST to an ntfy topic URL.",
                "type": "string"
              },
              {
                "const": "email",
                "description": "Resend's REST API; `target` is the recipient address.",
                "type": "string"
              }
            ]
          },
          "Severity": {
            "description": "How loud an alert is. Ordered, so a channel's `min_severity` is a `>=` comparison.",
            "enum": [
              "info",
              "warning",
              "critical"
            ],
            "type": "string"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One routing destination. `project_id: None` means **global** — it receives every project's\nalerts, which is exactly what the env-configured channels have always been.",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.484231800Z",
            "format": "date-time",
            "type": "string"
          },
          "enabled": {
            "default": true,
            "type": "boolean"
          },
          "id": {
            "default": "56746ddf-a830-4874-a5d5-60bcf2181925",
            "type": "string"
          },
          "kind": {
            "$ref": "#/$defs/ChannelKind"
          },
          "kinds": {
            "description": "Which kinds this channel wants. Empty = every kind.",
            "items": {
              "$ref": "#/$defs/AlertKind"
            },
            "type": "array"
          },
          "min_severity": {
            "$ref": "#/$defs/Severity",
            "default": "info"
          },
          "prev_secret_hash": {
            "description": "The previous signing key, kept live through a rotation so in-flight receivers that have not\npicked up the new secret still verify. Deliveries carry a `v1=` value for each.",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "secret_hash": {
            "description": "The **derived signing key** for this channel's webhook signature: `sha256(secret)` as hex.\n\nThe plaintext secret is minted server-side, returned exactly once on create (mirroring the\nAPI-key show-once pattern) and never stored, so a database leak does not hand out the shared\nsecret an operator may have reused. A receiver derives the same key from the secret it was\nshown. Never serialized outward — see [`AlertChannel::redacted`].",
            "type": [
              "string",
              "null"
            ]
          },
          "target": {
            "description": "The URL (webhook/ntfy) or address (email) alerts are sent to.",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "target"
        ],
        "title": "AlertChannel",
        "type": "object"
      },
      "Benchmark": {
        "$defs": {
          "BenchmarkCase": {
            "description": "One case in a benchmark dataset. `output` is the candidate to judge; `expected` is an optional\nreference answer the judge can compare against.",
            "properties": {
              "expected": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "input": {
                "type": "string"
              },
              "output": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "input"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A benchmark definition: a dataset + rubric + judge run repeatedly to track quality over time.",
        "properties": {
          "baseline_score": {
            "description": "Baseline mean score to detect regressions against.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.484512100Z",
            "format": "date-time",
            "type": "string"
          },
          "dataset": {
            "description": "Inline dataset of cases.",
            "items": {
              "$ref": "#/$defs/BenchmarkCase"
            },
            "type": "array"
          },
          "dataset_ref": {
            "description": "Reference to an external case dataset (path/URI/table), if not inlined in `dataset`.",
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "default": "2e5ffec3-25db-48e7-b106-2735996ad92a",
            "type": "string"
          },
          "judge_model": {
            "default": "haiku",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "rubric": {
            "type": "string"
          },
          "rubric_id": {
            "description": "Optional structured rubric (id) for per-dimension judging; falls back to `rubric` text if unset.",
            "type": [
              "string",
              "null"
            ]
          },
          "target": {
            "description": "How to produce outputs to judge (e.g. an endpoint, a model+prompt). Free-form for now."
          }
        },
        "required": [
          "name",
          "rubric"
        ],
        "title": "Benchmark",
        "type": "object"
      },
      "BenchmarkRun": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One execution of a [`Benchmark`].",
        "properties": {
          "benchmark_id": {
            "type": "string"
          },
          "cost_usd": {
            "default": 0.0,
            "format": "double",
            "type": "number"
          },
          "finished_at": {
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "default": "bae6b350-ef71-4d33-a288-796798d5c290",
            "type": "string"
          },
          "mean_score": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "n_cases": {
            "default": 0,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "p50_latency_ms": {
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "p95_latency_ms": {
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "pass_rate": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "report": {
            "description": "Phase 3.6c: per-dimension breakdown + recommendations/healing for this run."
          },
          "started_at": {
            "default": "2026-09-03T12:23:35.484651200Z",
            "format": "date-time",
            "type": "string"
          },
          "status": {
            "default": "completed",
            "description": "The runner's verdict for the run. Minted by the runner's `significance_verdict` and\n`aggregate_status`: `passed` | `regressed` | `no_baseline`, or `partial` (a cost ceiling cut\nthe run) | `cancelled` (an operator did); `running` marks a run whose finish never arrived,\nand `completed` is the serde default a row with no status reads as. The four-word list this\ndoc used to carry predated every status but the first two.",
            "type": "string"
          },
          "total_tokens": {
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "benchmark_id"
        ],
        "title": "BenchmarkRun",
        "type": "object"
      },
      "CalibrationRecord": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One completed judge↔human calibration, stored.",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.484896400Z",
            "format": "date-time",
            "type": "string"
          },
          "dataset_id": {
            "description": "The labeled set this was measured on, when it came from one rather than from a file.",
            "type": [
              "string",
              "null"
            ]
          },
          "dataset_version": {
            "description": "The dataset generation measured, so a record does not silently claim to describe a set that\nhas since been added to. Written by whoever knows the generation; `None` on a file import.",
            "format": "uint32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "id": {
            "default": "897a819e-c730-481b-b187-a444e37270f8",
            "type": "string"
          },
          "judge": {
            "description": "The judge that was measured, as a canonical `[provider/]model` string. Half of the key: the\nsame rubric calibrated against two judges is two independent facts, and trusting one because\nthe other passed is exactly the uncalibrated-gate failure.",
            "type": "string"
          },
          "kappa": {
            "format": "double",
            "type": "number"
          },
          "kappa_bar": {
            "description": "The bar `kappa` was compared against, stored beside it: raising the bar later must not\nsilently re-verdict history.",
            "format": "double",
            "type": "number"
          },
          "mae": {
            "format": "double",
            "type": "number"
          },
          "n": {
            "description": "Pairs measured. A record with a small `n` is still a record — the honest thing is to store\nit with its `n` rather than to refuse it, so a reader can see that the trust rests on 12\ncases (D15's own caveat) instead of guessing.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "pearson": {
            "format": "double",
            "type": "number"
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "rmse": {
            "format": "double",
            "type": "number"
          },
          "rubric_id": {
            "description": "The rubric the judging was done under. `None` = a freeform (rubric-less) calibration, which\nanswers only for freeform judging — a rubric's own trust is never inherited from it.",
            "type": [
              "string",
              "null"
            ]
          },
          "trusted": {
            "description": "`kappa >= kappa_bar` at the time of measurement.",
            "type": "boolean"
          }
        },
        "required": [
          "judge",
          "kappa",
          "pearson",
          "mae",
          "rmse",
          "n",
          "kappa_bar",
          "trusted"
        ],
        "title": "CalibrationRecord",
        "type": "object"
      },
      "CollectiveDigest": {
        "$defs": {
          "Coverage": {
            "description": "How uniformly a boolean rigor fact holds across the runs/sources behind a bucket or row.\n\n`All` / `None` are **complete** claims: every contributor recorded the fact and they agree. Anything\nshort of that is `Mixed` — including \"they agree but somebody didn't say\", because a claim resting\non silence is not a claim. `Unknown` is nobody said anything at all.",
            "oneOf": [
              {
                "const": "all",
                "description": "Every contributing run/source recorded the fact as true.",
                "type": "string"
              },
              {
                "const": "mixed",
                "description": "Recorded values disagree, or some were recorded and some were not.",
                "type": "string"
              },
              {
                "const": "none",
                "description": "Every contributing run/source recorded the fact as false.",
                "type": "string"
              },
              {
                "const": "unknown",
                "description": "Nothing recorded it.",
                "type": "string"
              }
            ]
          },
          "ModelDigestEntry": {
            "description": "A published digest entry: one `(provider, model, task_type)` bucket aggregated across an instance's\nruns. Purely aggregate — safe to share.",
            "properties": {
              "avg_cost_usd": {
                "description": "Mean cost per case, USD.",
                "format": "double",
                "type": "number"
              },
              "determinism": {
                "description": "v3: the **weakest** determinism stamp across the bucket's runs — a bucket is only as\nreproducible as its least reproducible run. `None` when any run recorded none.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "frozen_dataset": {
                "$ref": "#/$defs/Coverage",
                "description": "v3: whether the bucket's runs all ran against a frozen dataset pinned at a **single** version.\n`all` is a complete claim; `mixed` means the runs disagreed (or drifted across versions);\n`unknown` means no run recorded dataset provenance."
              },
              "judge_provider": {
                "description": "v2: coarse judge family for this bucket (`anthropic|openai|google|unknown`, or `mixed` when the\nbucket's runs disagree). Provider only — never the full judge model.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "model": {
                "type": "string"
              },
              "n_cases": {
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "n_runs": {
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "p50_latency_ms": {
                "format": "uint64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "p95_latency_ms": {
                "format": "uint64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "pass_rate": {
                "format": "double",
                "type": "number"
              },
              "provider": {
                "type": "string"
              },
              "quality": {
                "format": "double",
                "type": "number"
              },
              "quality_variance": {
                "description": "v2: population variance of `quality` across the contributing runs (case-weighted). `None` when\nthe bucket came from a single run (variance undefined) or from a v1 contributor.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "rubric_fingerprint": {
                "description": "v2: rubric-shape fingerprint (short one-way hash), or `mixed` when the bucket's runs were scored\nunder different rubrics (the same collapse as `judge_provider`). `None` when none was recorded.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "significance_tested": {
                "$ref": "#/$defs/Coverage",
                "description": "v3: whether the bucket's runs all carried a significance-tested verdict."
              },
              "task_type": {
                "type": "string"
              }
            },
            "required": [
              "provider",
              "model",
              "task_type",
              "quality",
              "pass_rate",
              "avg_cost_usd",
              "n_runs",
              "n_cases"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A full digest an instance contributes to a hub. The `contributor_id` is **opaque** (a hash) but a\nhub ignores it and derives identity from the presented bearer key; it stays on the wire only for\nbackward compatibility.",
        "properties": {
          "contributor_id": {
            "default": "anonymous",
            "type": "string"
          },
          "entries": {
            "default": [],
            "items": {
              "$ref": "#/$defs/ModelDigestEntry"
            },
            "type": "array"
          },
          "generated_at": {
            "default": "2026-09-03T12:23:35.485018400Z",
            "format": "date-time",
            "type": "string"
          },
          "min_cases": {
            "default": 0,
            "description": "The k-anonymity floor used to build this digest (for auditability).",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "projects_excluded": {
            "default": 0,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "projects_included": {
            "default": 0,
            "description": "Consent envelope: how many projects opted into this digest, and how many were withheld for\nlacking `collective_opt_in`. Makes what leaves the building legible *before* the POST.\nSerde-defaulted so v1/v2 hubs (which ignore unknown fields) stay wire-compatible.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "schema_version": {
            "default": 3,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "CollectiveDigest",
        "type": "object"
      },
      "ContributionRecord": {
        "$defs": {
          "ContributionStatus": {
            "description": "How a contribution attempt ended. Three outcomes, because the operator's next move differs for\neach: `Sent` is done, `Rejected` means the hub understood and declined (fix the digest or the\ncredential), `Failed` means the push never landed (retry).",
            "oneOf": [
              {
                "const": "sent",
                "description": "The hub accepted it (2xx).",
                "type": "string"
              },
              {
                "const": "rejected",
                "description": "The hub answered, and refused (4xx/5xx) — including the `429` a `min_interval` produces.",
                "type": "string"
              },
              {
                "const": "failed",
                "description": "The push never got an answer: DNS, TLS, connect, timeout.",
                "type": "string"
              }
            ]
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One recorded contribution attempt.",
        "properties": {
          "ack": {
            "description": "The hub's answer, verbatim (or a structured error for [`ContributionStatus::Failed`])."
          },
          "contributor_id_as_acked": {
            "description": "The contributor id the **hub** said it filed this under. `None` when the hub's ack carried\nnone (an older hub, or a refusal). Worth keeping apart from this instance's own preview id:\na hub derives identity from the presented key, so the two can legitimately differ, and an\noperator chasing \"why are our rows not merging\" needs the hub's answer, not ours.",
            "type": [
              "string",
              "null"
            ]
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.485428900Z",
            "format": "date-time",
            "type": "string"
          },
          "digest_sha256": {
            "description": "[`digest_sha256`] of the digest body. The gate a repeat push is skipped by.",
            "type": "string"
          },
          "entries_count": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "generated_at": {
            "description": "When the digest this row describes was *built* (not when it was sent).",
            "format": "date-time",
            "type": "string"
          },
          "hub_url_hash": {
            "description": "Opaque, stable hash of the hub's base URL — see the module docs for why it is not the URL.",
            "type": "string"
          },
          "id": {
            "default": "2b4887be-4d67-4101-aa96-2fd19cd9e05f",
            "type": "string"
          },
          "projects_excluded": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "projects_included": {
            "description": "The consent envelope, at rest. `GET /digest` recomputes these per call, so before this row\nexisted the record of *which* projects consented to a push lived only on the wire.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "schema_version": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "status": {
            "$ref": "#/$defs/ContributionStatus"
          }
        },
        "required": [
          "hub_url_hash",
          "schema_version",
          "generated_at",
          "entries_count",
          "projects_included",
          "projects_excluded",
          "digest_sha256",
          "status"
        ],
        "title": "ContributionRecord",
        "type": "object"
      },
      "CostByDimension": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "LLM cost aggregated for one dimension value over the window (produced by the store from events).",
        "properties": {
          "calls": {
            "format": "int64",
            "type": "integer"
          },
          "cost_usd": {
            "format": "double",
            "type": "number"
          },
          "key": {
            "description": "The customer/product id (or prompt tag), or `None` for untagged (unattributed) cost.",
            "type": [
              "string",
              "null"
            ]
          },
          "unpriced_calls": {
            "default": 0,
            "description": "How many of `calls` had no price on the row, so they contributed `$0.00` to `cost_usd`. The\ndisclosure that keeps this row from reading as a complete number when it is a floor: a\nmargin computed from a bucket with unpriced traffic understates cost by an unknown amount.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "calls",
          "cost_usd"
        ],
        "title": "CostByDimension",
        "type": "object"
      },
      "Dataset": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A versioned, reusable evaluation dataset. Freezing makes the dataset immutable, which fixes\none half of run comparability — the input. It does not fix the other half. Where the cases came\nfrom outside this project (`source: import`), the models under test accumulate exposure to that\nmaterial as it circulates, so two runs of the same frozen dataset months apart are not\ninterchangeable: a rise can be the target or judge having *seen* the cases rather than having\nimproved. Cases sampled from this project's own traffic (`source: events:recent`) cannot be\nexposed that way, which is the strongest reason to prefer them over an import.",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.485676500Z",
            "format": "date-time",
            "type": "string"
          },
          "frozen": {
            "default": false,
            "type": "boolean"
          },
          "id": {
            "default": "9135c83f-f14b-4aa4-a8a2-f5a1c43574a6",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "parent_id": {
            "description": "The dataset this one was forked from (M24), when it was forked rather than created.\n\nThe link is what makes `version` mean anything: without it a v2 is just another row that\nhappens to share a name, and \"is this run's corpus the same corpus as that run's\" has no\nanswer beyond string equality. Absent on every dataset created directly.",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "source": {
            "description": "Provenance, e.g. `events:recent`, `manual`, `import`.",
            "type": [
              "string",
              "null"
            ]
          },
          "version": {
            "default": 1,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "name"
        ],
        "title": "Dataset",
        "type": "object"
      },
      "DatasetItem": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One case in a dataset. `output` is a captured/candidate response; `expected` is a golden reference.",
        "properties": {
          "anonymization": {
            "description": "Anonymization audit, e.g. `{\"method\":\"regex+llm\",\"redactions\":3}`."
          },
          "context": {
            "type": [
              "string",
              "null"
            ]
          },
          "dataset_id": {
            "default": "",
            "type": "string"
          },
          "expected": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "default": "0b3d2dc8-c001-4002-bcbe-78f1a1c3f65a",
            "type": "string"
          },
          "input": {
            "type": "string"
          },
          "input_hash": {
            "description": "Fingerprint of the normalised `input` (M24), stored so near-duplicate collapse is an index\nlookup instead of a scan of every case's text. Absent on items written before the column\nexisted and on backends that do not compute it — which is why dedupe treats a `NULL` as \"not\nknown to be a duplicate\" rather than as a match.",
            "type": [
              "string",
              "null"
            ]
          },
          "output": {
            "type": [
              "string",
              "null"
            ]
          },
          "source_event_id": {
            "description": "The real event this item was sampled from, if any.",
            "type": [
              "string",
              "null"
            ]
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "input"
        ],
        "title": "DatasetItem",
        "type": "object"
      },
      "Device": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One enrolled device that may lease relay tasks.\n\n`key_hash` follows the API-key scheme verbatim (`\"<salt>:<sha256hex>\"`), and the raw key —\n`ltd_<prefix>_<secret>` — is returned once at creation and never stored. `key_prefix` is the\nnon-secret lookup handle.",
        "properties": {
          "agent_version": {
            "description": "The `lt-agent` version this device last reported, so an operator can tell a fleet that has\nnot been upgraded from one that has.",
            "type": [
              "string",
              "null"
            ]
          },
          "capabilities": {
            "default": [],
            "description": "What this device can run: exact action types, or `\"<ns>/*\"` namespace prefixes. An **empty**\nlist means \"everything\", which is what an agent predating M18 (and the legacy shared key)\nadvertises — a device that suddenly leased nothing after an upgrade would be a worse failure\nthan an unfiltered one.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.485943900Z",
            "format": "date-time",
            "type": "string"
          },
          "id": {
            "default": "9e3ddef1-3459-4906-8b18-4bb46f2c0fe3",
            "type": "string"
          },
          "key_hash": {
            "default": "",
            "description": "Salted SHA-256 of the full key. **Never** serialized outward — the API strips it before it\nreaches a response, and this type is what the store round-trips.",
            "type": "string"
          },
          "key_prefix": {
            "type": "string"
          },
          "last_seen_at": {
            "description": "When this device last leased, renewed, or reported. The liveness signal\n`GET /v1/relay/devices` shows: there is no inbound path to a device, so \"is it alive\" can\nonly ever be \"when did it last talk to us\".",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "project_id": {
            "description": "Which project this device belongs to, or `None` for an **operator-wide** device that may\nserve every project's tasks. Operator-wide is the shape the shipped single-device relay\nalready had, so it stays expressible rather than forcing an artificial project on it.",
            "type": [
              "string",
              "null"
            ]
          },
          "revoked": {
            "default": false,
            "description": "A revoked device authenticates nothing and is eligible for nothing. Revocation is a flag,\nnot a delete, so the tasks it already ran keep naming a device that still exists.",
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "key_prefix"
        ],
        "title": "Device",
        "type": "object"
      },
      "Job": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A unit of background work (Phase 3.6d). Enqueued by the API, executed by `lt-runner serve`,\nso long operations (benchmark runs) never block ingestion. Cloud path swaps the table for Pub/Sub.",
        "properties": {
          "attempts": {
            "default": 0,
            "description": "How many times a worker has CLAIMED this job. Bumped inside the atomic claim, so it counts\ncrashes too — which is why it is no longer what decides a retry (see `failures`).",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "claimed_at": {
            "description": "When the holding worker last proved it is alive — set by the claim and moved forward by\nevery lease renewal.\n\nIt is also this job's **fencing token**. A claim (or a reclaim) stamps a new value here, so\na worker that holds `claimed_at = T` and finds the row no longer at `T` has learned that\nsomeone else owns the job now. Every write a worker makes about a job it believes it holds\ncarries this value and is refused if it does not match — see [`crate::JobFinish`]. That is\nwhat stops a slow worker, reclaimed while it was busy, from overwriting the verdict its\nreplacement already wrote.\n\nBecause it now moves on renewal rather than only on claim, the staleness window is a\n**detection latency** (how long a dead worker may go unnoticed — minutes) and no longer has\nto be sized to the longest legitimate job (hours). Those are the two quantities a single\nclaim timestamp used to conflate.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.486190700Z",
            "format": "date-time",
            "type": "string"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "failures": {
            "default": 0,
            "description": "How many times the job actually RAN and failed (the worker reported an error). This — not\n`attempts` — is the retry budget, so a worker that dies mid-run no longer burns one of the\njob's three chances.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "id": {
            "default": "1a74d60d-fa6d-463e-ae6c-104f4e189db3",
            "type": "string"
          },
          "max_attempts": {
            "default": 3,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "payload": true,
          "progress": {
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "description": "The tenant this work belongs to, stamped at enqueue from the benchmark/schedule it runs.\n\n`None` is an operator/legacy job — a sweep the deployment itself enqueued, or a row written\nbefore the column existed. Only an operator scope reads those; a project key never does\n(M17), which is what stops `GET /v1/jobs` from being a cross-tenant payload dump.",
            "type": [
              "string",
              "null"
            ]
          },
          "result": true,
          "stale_reclaims": {
            "default": 0,
            "description": "How many times this job was reclaimed after a worker held it past the stale window without\nfinishing: the count of *worker deaths*, kept apart from `failures` so an operator can tell\n\"the judge failed every case\" from \"the worker was killed\".",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "status": {
            "default": "queued",
            "description": "`queued` | `running` | `cancelling` | `cancelled` | `done` | `failed`.\n\n`cancelling` is a *running* job an operator asked to stop: the worker notices at its next\ncase boundary and finishes it as `cancelled`. It is deliberately outside the claimable set,\nso the stale-claim reclaim path can never restart a run someone cancelled.",
            "type": "string"
          },
          "type": {
            "description": "Job kind, e.g. `bench_run`.",
            "type": "string"
          },
          "updated_at": {
            "default": "2026-09-03T12:23:35.486203700Z",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "title": "Job",
        "type": "object"
      },
      "Label": {
        "$defs": {
          "LabelSubject": {
            "description": "What a [`Label`] is about.\n\nThree subjects rather than one free-text `(kind, id)` pair, because each one is a different\nquestion and the store indexes them together: an event is production traffic a human graded, a\ndataset item is a curated golden case, and a score is a human *reviewing the judge* — the\ndisagreement signal `GET /v1/scores?needs_review=1` reads.",
            "oneOf": [
              {
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "type": {
                    "const": "event",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "id"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "type": {
                    "const": "dataset_item",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "id"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "type": {
                    "const": "score",
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "id"
                ],
                "type": "object"
              }
            ]
          },
          "ScoreDim": {
            "description": "One dimension's contribution to a judged verdict, kept so a stored score can answer *why*.",
            "properties": {
              "floor": {
                "description": "The rubric's gating floor for this dimension, when it has one.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "floor_hit": {
                "default": false,
                "description": "True when `value` fell below `floor` — the reason a high overall can still fail. On a\nmerged cell this is the OR across the cells merged, so it can be true while the merged\n(mean) `value` clears the floor; read `floor_hits`/`floor_of` for the shape.",
                "type": "boolean"
              },
              "floor_hits": {
                "description": "How many contributing observations fell below `floor`, and how many there were. One\njudged verdict reports its own; a merged cell reports the tally across what it merged.\n\n`floor_hit` alone cannot separate a dimension that crossed on **every** observation from\none that crossed on **one of five**, because an OR reduces both to `true` — and that is\nexactly the difference between a boundary the candidate always hits and one it sometimes\ndoes. The count carries its own denominator so the two are never confused.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "floor_of": {
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "key": {
                "type": "string"
              },
              "reasoning": {
                "description": "The judge's reasoning, one entry per sample that parsed (sample order). Every sample is kept:\nits reasoning tokens were paid for.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "value": {
                "description": "The aggregated (mean-over-samples) dimension score.",
                "format": "double",
                "type": "number"
              },
              "weight": {
                "format": "double",
                "type": "number"
              }
            },
            "required": [
              "key",
              "value",
              "weight"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One human verdict.\n\n`value` is normalized to 0..1 exactly as [`crate::Score::value`] is over `max`, so a label and a\njudge verdict on the same subject are directly comparable — which is the entire point, and the\nreason there is no `max` here to get out of step with the judge's.",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.486939Z",
            "format": "date-time",
            "type": "string"
          },
          "dimensions": {
            "description": "Per-dimension human scores, in the same shape the judge reports them, so a per-dimension\ndisagreement is a subtraction rather than a schema translation.",
            "items": {
              "$ref": "#/$defs/ScoreDim"
            },
            "type": "array"
          },
          "id": {
            "default": "8243f748-6b8b-498c-954c-786e28bb629c",
            "type": "string"
          },
          "labeler": {
            "description": "Who said so. Free text (a person, a team, `\"import:<file>\"`) — the provenance that makes a\ncalibration result auditable at all, which is why it is required rather than optional.",
            "type": "string"
          },
          "note": {
            "type": [
              "string",
              "null"
            ]
          },
          "pass": {
            "description": "The human's pass/fail call, when they made one explicitly. `None` means \"derive it from\n`value` against the rubric's threshold\" — kept distinct so a labeler who deliberately passed\na 0.4 case is not overruled by a threshold.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "project_id": {
            "default": "",
            "description": "Defaulted so a keyed poster can omit it (the API derives it from the API key).",
            "type": "string"
          },
          "rubric_id": {
            "description": "The rubric this opinion was formed under, when there was one. A label with no rubric is a\ngeneral quality opinion and calibrates any rubric; one with a rubric calibrates only that\nrubric, because \"good\" means a different thing under a different set of criteria.",
            "type": [
              "string",
              "null"
            ]
          },
          "subject": {
            "$ref": "#/$defs/LabelSubject"
          },
          "value": {
            "description": "Overall quality in 0..1.",
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "subject",
          "value",
          "labeler"
        ],
        "title": "Label",
        "type": "object"
      },
      "LeaseHeld": {
        "$defs": {
          "LeaseFence": {
            "description": "The identity of one lease: the instant it was granted, compared for **exact equality**.\n\nA newtype rather than a bare `DateTime` on purpose. The two timestamps on a leased row — \"when\ndid this lease start\" and \"when does it expire\" — are the same Rust type and are trivially\nswappable at a call site, and swapping them turns a fence check into a liveness check that\naccepts any holder. Naming the fence makes that mistake a compile error.",
            "format": "date-time",
            "type": "string"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "What a conditioned write against a lease actually did.\n\n`NotHeld` is deliberately a *value*, not an error: losing a lease is a normal outcome in a\ndistributed queue and the holder has to act on it (stop working, do not deliver, do not retry),\nwhich it cannot do if the answer is indistinguishable from a transient failure. The payload says\nwhat the record holds **now**, so the loser can name what beat it.",
        "oneOf": [
          {
            "description": "The write landed. `deadline` is the lease's new expiry when the call extended it.",
            "properties": {
              "deadline": {
                "format": "date-time",
                "type": [
                  "string",
                  "null"
                ]
              },
              "outcome": {
                "const": "held",
                "type": "string"
              }
            },
            "required": [
              "outcome"
            ],
            "type": "object"
          },
          {
            "description": "Refused: the caller does not hold this record any more.",
            "properties": {
              "fence": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/LeaseFence"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "outcome": {
                "const": "not_held",
                "type": "string"
              },
              "status": {
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "status"
            ],
            "type": "object"
          },
          {
            "description": "There is no such record (→ 404). Distinct from `NotHeld`, which is a live one someone else owns.",
            "properties": {
              "outcome": {
                "const": "no_such_record",
                "type": "string"
              }
            },
            "required": [
              "outcome"
            ],
            "type": "object"
          }
        ],
        "title": "LeaseHeld"
      },
      "LimitRule": {
        "$defs": {
          "Escalation": {
            "description": "A temporary, forecast-driven tightening of a rule's action.\n\nThe measure→act gap this closes: `budget_breach` alerts already know a project breaches in ~2\ndays, and the only thing that ever happened with that knowledge was a Slack message. An\nescalation says what to *do* — \"when the ETA drops under 2 days, throttle for 12 hours\" — and the\nsweep applies it.\n\nReversal is what makes it safe: the configured [`LimitRule::action`](super::LimitRule::action) is\nnever overwritten, only shadowed until [`LimitRule::escalated_until`](super::LimitRule) passes, so\nde-escalation is a field clear rather than a remembered undo.",
            "properties": {
              "for_hours": {
                "description": "How long one escalation lasts before it lapses on its own, even if nothing sweeps again.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "on_eta_days": {
                "description": "Escalate once the forecast ETA to breach is at or under this many days.",
                "format": "double",
                "type": "number"
              },
              "to": {
                "$ref": "#/$defs/LimitAction",
                "description": "The action to apply while escalated."
              }
            },
            "required": [
              "on_eta_days",
              "to",
              "for_hours"
            ],
            "type": "object"
          },
          "LimitAction": {
            "description": "What happens as a limit is approached and breached. Three genuinely distinct tiers:\n\n- **`Alert`** — observe-only. Notifies; never rejects anything.\n- **`Throttle`** — *graduated*. Below [`LimitRule::throttle_start`] nothing happens. Between that\n  ratio and the threshold a proportionally growing share of ingest is shed (HTTP 429 with a short\n  `Retry-After`), so a client feels back-pressure and slows down *before* the wall instead of\n  going from fully accepted to fully rejected between two consecutive events. At and above the\n  threshold it is a hard stop, identical to `Block`.\n- **`Block`** — an unambiguous hard stop at the threshold, with no shedding beforehand. A strict\n  cap stays strict.\n\nBoth enforcing tiers reject at ingest admission (the event is not recorded). Pre-call refusal —\nstopping the provider call before a token is bought — is the SDKs' pre-spend admission\n(`gate()` in the Rust/Python/TS clients), decided from the last ingest response's proximity\nsignal; it mirrors these tiers, it does not replace them.",
            "enum": [
              "alert",
              "throttle",
              "block"
            ],
            "type": "string"
          },
          "LimitMetric": {
            "description": "What a limit measures over its window.",
            "enum": [
              "cost_usd",
              "calls",
              "tokens"
            ],
            "type": "string"
          },
          "LimitScope": {
            "description": "Optional dimension a limit is scoped to — provider / model / use-case (`name`) / API key /\nbilling customer. An unscoped rule (`None` on [`LimitRule::scope`]) applies to the whole project,\nexactly as before; a scoped rule only counts (and can reject) traffic matching the selected\ndimension value, so an operator can \"cap gpt-4o at $5/day\", \"cap use-case X\", or give a staging\nkey $5/day while production keeps $500 — without touching other traffic.\n\nSerializes externally-tagged, e.g. `{\"model\":\"gpt-4o\"}` / `{\"provider\":\"openai\"}` /\n`{\"name\":\"summarize\"}` / `{\"api_key\":\"<key-id>\"}` / `{\"customer\":\"cus_123\"}`.",
            "oneOf": [
              {
                "additionalProperties": false,
                "properties": {
                  "provider": {
                    "type": "string"
                  }
                },
                "required": [
                  "provider"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "properties": {
                  "model": {
                    "type": "string"
                  }
                },
                "required": [
                  "model"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "description": "Use-case, matched against an event's `name`.",
                "properties": {
                  "name": {
                    "type": "string"
                  }
                },
                "required": [
                  "name"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "description": "One API key, identified by its **row id** — the opaque primary key of the `api_keys` table.\n\nDeliberately *not* the key material, and not a hash of it: the id is generated independently\nof the secret, so nothing about the key can be recovered from a rule, a status payload, or an\nalert. The non-secret `prefix` would also have worked, but the id is what the keys API already\nreturns and what the event carries, so there is exactly one identifier to reason about.",
                "properties": {
                  "api_key": {
                    "type": "string"
                  }
                },
                "required": [
                  "api_key"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "description": "One billing customer, matched against `metadata.customer_id` — the same linkage margin\nanalytics already group on.",
                "properties": {
                  "customer": {
                    "type": "string"
                  }
                },
                "required": [
                  "customer"
                ],
                "type": "object"
              }
            ]
          },
          "LimitWindow": {
            "description": "Rolling window a limit is evaluated over.",
            "enum": [
              "hour",
              "day",
              "month"
            ],
            "type": "string"
          },
          "Threshold": {
            "anyOf": [
              {
                "format": "double",
                "type": "number"
              },
              {
                "properties": {
                  "dimension": {
                    "$ref": "#/$defs/ThresholdDimension",
                    "default": "customer"
                  },
                  "pct": {
                    "description": "Percentage of recognized revenue, in `(0, 1000]`. Over 100 is legitimate — \"you may burn\nup to 3x what this customer pays while we buy the land-grab\" is a policy, not a typo.",
                    "format": "double",
                    "type": "number"
                  }
                },
                "required": [
                  "pct"
                ],
                "type": "object"
              }
            ],
            "description": "What a rule's limit *is*: a constant, or a share of what the subject actually pays us.\n\n`#[serde(untagged)]` with `Fixed` first: a stored `\"threshold\": 5.0` still reads as\n`Fixed(5.0)`, and `{\"pct\": 80, \"dimension\": \"customer\"}` reads as `RevenueShare`. Order matters —\nan object can never match `Fixed`, and a number can never match `RevenueShare`, so the two arms\nare unambiguous."
          },
          "ThresholdDimension": {
            "description": "The billing dimension a [`Threshold::RevenueShare`] reads revenue over. One variant today — the\ncustomer — because that is the only dimension `list_revenue_events` can attribute without\nguessing; it is an enum so adding `Product` later is additive rather than a wire break.",
            "enum": [
              "customer"
            ],
            "type": "string"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A per-project limit. Tripped by **monitored traffic only** — the scoring engine is exempt.",
        "properties": {
          "action": {
            "$ref": "#/$defs/LimitAction",
            "description": "The rule's **configured** reaction. While an escalation is live this is shadowed by\n[`Escalation::to`] rather than overwritten, so de-escalation is a field clear and never a\nremembered undo — see [`LimitRule::effective_action`]."
          },
          "enabled": {
            "default": true,
            "type": "boolean"
          },
          "escalated_until": {
            "description": "When a live escalation lapses. Set by the sweep, cleared by the sweep's reverse pass, and\nself-expiring so a sweep that stops running cannot leave a project throttled forever.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "escalation": {
            "anyOf": [
              {
                "$ref": "#/$defs/Escalation"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional forecast-driven tightening: when the budget ETA drops under\n[`Escalation::on_eta_days`], the sweep stamps [`LimitRule::escalated_until`] and this rule\nacts as [`Escalation::to`] until it lapses. `None` (serde-default) = the pre-escalation\nbehaviour, unchanged."
          },
          "expires_at": {
            "description": "When a policy-created rule stops applying. Past it the rule is inert (and the sweep deletes\nit), so a guardrail raised by automation cannot outlive the condition that raised it even if\nthe sweep is turned off.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "type": "string"
          },
          "metric": {
            "$ref": "#/$defs/LimitMetric"
          },
          "origin": {
            "description": "What created this rule, when it was not a human: `margin_policy:<policy id>:<subject>`. The\npolicy engine only ever touches rules carrying its own origin, which is what keeps an\noperator's hand-made cap safe from automation.",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "type": "string"
          },
          "scope": {
            "anyOf": [
              {
                "$ref": "#/$defs/LimitScope"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Optional dimension this rule caps (provider / model / use-case). `None` (serde-default) =\nproject-wide, byte-identical to pre-scope behavior. A scoped rule only counts and rejects\ntraffic matching its scope."
          },
          "threshold": {
            "$ref": "#/$defs/Threshold",
            "description": "The number usage is compared against — a constant, or a share of recognized revenue that is\nresolved at evaluation time (see [`Threshold`]). Untagged, so a stored bare number still\ndeserializes to `Fixed` with byte-identical behaviour."
          },
          "warn_at": {
            "default": null,
            "description": "Optional soft-warning tier: a fraction of the threshold in `(0, 1)`. When rolling usage\nreaches `ratio >= warn_at` *without* breaching, a distinct `limit_warning` alert fires (its\nown cooldown) so the operator hears about an approaching cap before the 429. `None` = no\npre-warning (old rules deserialize to this, unchanged). Never enforces.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "window": {
            "$ref": "#/$defs/LimitWindow"
          }
        },
        "required": [
          "id",
          "project_id",
          "metric",
          "window",
          "threshold",
          "action"
        ],
        "title": "LimitRule",
        "type": "object"
      },
      "LlmEvent": {
        "$defs": {
          "Operation": {
            "description": "The kind of operation. `Other` catches anything unmodeled.",
            "enum": [
              "chat",
              "completion",
              "embedding",
              "other"
            ],
            "type": "string"
          },
          "ProviderId": {
            "description": "A canonical provider id — lowercase, trimmed, restricted to `[a-z0-9._-]`.\n\nCanonicalized on construction *and* on deserialize, so a wire value, a DB column, and a\nhand-written price row all reach the same string. Nothing is mapped onto anything else here: an\nid is preserved verbatim once canonicalized (`mistral` stays `mistral`, `az.ai.openai` stays\n`az.ai.openai`). Synonym folding is a separate, declared step in\n[`crate::model_id::canonicalize`].",
            "type": "string"
          },
          "Status": {
            "description": "Call outcome.",
            "enum": [
              "success",
              "error",
              "timeout"
            ],
            "type": "string"
          },
          "TokenUsage": {
            "description": "Token accounting for a single call.\n\n**The convention, stated once:** `input` is the WHOLE prompt the provider counted, and\n`cached_input` is the part of it served from a prompt cache — a subset, never an addition. The\nprice book bills `input − cached_input` at the input rate and `cached_input` at the cached rate,\nso a sender that reports cached tokens *beside* rather than *within* `input` under-bills every\ncache hit. Providers differ on the wire (OpenAI's `prompt_tokens` includes cached tokens;\nAnthropic's `input_tokens` excludes `cache_read_input_tokens`), and it is each extractor's job to\nconvert to this shape before the event is sent. `reasoning` is informational: providers count it\ninside `output`, which is where it is billed.",
            "properties": {
              "cached_input": {
                "format": "uint64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "input": {
                "default": 0,
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              },
              "output": {
                "default": 0,
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              },
              "reasoning": {
                "format": "uint64",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One normalized LLM call — the canonical record everything else is derived from.",
        "properties": {
          "cost_usd": {
            "description": "Provider-reported cost if known; otherwise filled by [`LlmEvent::ensure_cost`].",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "default": "4f5e6d3c-7f7e-4c27-8f79-bea3d539e9dc",
            "type": "string"
          },
          "input": {
            "description": "Optional, redactable payloads."
          },
          "latency_ms": {
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "metadata": true,
          "model": {
            "type": "string"
          },
          "name": {
            "description": "Optional use-case / call-site name (e.g. \"summarize-email\"). The unit the\nPersonas \"LLM Overview\" rollup groups by; falls back to provider+model when\nabsent. Set it per call via the SDK (`track(..., name=...)`).",
            "type": [
              "string",
              "null"
            ]
          },
          "operation": {
            "$ref": "#/$defs/Operation",
            "default": "chat"
          },
          "output": true,
          "parent_span_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "default": "",
            "description": "Defaulted so keyed ingest can omit it (the API derives it from the API key).",
            "type": "string"
          },
          "provider": {
            "$ref": "#/$defs/ProviderId",
            "default": "unknown"
          },
          "source": {
            "type": [
              "string",
              "null"
            ]
          },
          "span_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/$defs/Status",
            "default": "success"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "trace_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "ts": {
            "default": "2026-09-03T12:23:35.488547100Z",
            "format": "date-time",
            "type": "string"
          },
          "usage": {
            "$ref": "#/$defs/TokenUsage",
            "default": {
              "input": 0,
              "output": 0
            }
          }
        },
        "required": [
          "model"
        ],
        "title": "LlmEvent",
        "type": "object"
      },
      "MarginPolicy": {
        "$defs": {
          "PolicyAction": {
            "description": "What a fired policy does about it.",
            "oneOf": [
              {
                "const": "warn",
                "description": "Raise an observe-only rule. The default stance: a guardrail an operator has not yet trusted\nshould tell them what it *would* have done.",
                "type": "string"
              },
              {
                "additionalProperties": false,
                "description": "Cap spend at `factor` times the customer's recognized revenue — a\n[`Threshold::RevenueShare`] rule, so the cap re-derives itself every invoice instead of going\nstale.",
                "properties": {
                  "cap_to_revenue": {
                    "properties": {
                      "factor": {
                        "format": "double",
                        "type": "number"
                      }
                    },
                    "required": [
                      "factor"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "cap_to_revenue"
                ],
                "type": "object"
              },
              {
                "const": "throttle",
                "description": "Throttle at the customer's current cost (graduated shedding up to it).",
                "type": "string"
              },
              {
                "const": "block",
                "description": "Hard-stop at the customer's current cost.",
                "type": "string"
              }
            ]
          },
          "PolicyTrigger": {
            "description": "What makes a policy fire for one customer.",
            "oneOf": [
              {
                "additionalProperties": false,
                "description": "Gross margin percentage under this value (e.g. `20` → margin% < 20%). A cost-only row (no\nrevenue, so no margin%) qualifies when it is losing money — the free-tier sink is exactly the\ncase worth catching.",
                "properties": {
                  "below_pct": {
                    "format": "double",
                    "type": "number"
                  }
                },
                "required": [
                  "below_pct"
                ],
                "type": "object"
              },
              {
                "const": "negative_margin",
                "description": "Margin is already negative.",
                "type": "string"
              },
              {
                "additionalProperties": false,
                "description": "Not yet unprofitable, but forecast to turn unprofitable within this many days.",
                "properties": {
                  "erosion_eta_days": {
                    "format": "double",
                    "type": "number"
                  }
                },
                "required": [
                  "erosion_eta_days"
                ],
                "type": "object"
              }
            ]
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A standing margin guardrail for one project.",
        "properties": {
          "action": {
            "$ref": "#/$defs/PolicyAction"
          },
          "cooldown_secs": {
            "default": 3600,
            "description": "Minimum seconds between two applications for the same subject. Enforced by the caller (the\nsweep), which is the only thing that knows when it last acted.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "enabled": {
            "default": true,
            "type": "boolean"
          },
          "expiry_secs": {
            "default": 86400,
            "description": "How long a rule this policy creates stays in force before expiring on its own.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "id": {
            "type": "string"
          },
          "min_cost_usd": {
            "default": 0.0,
            "description": "Floor on the subject's windowed LLM cost before the policy acts. Without it a customer who\ncost us four cents and paid nothing would get a guardrail — noise that trains operators to\nignore the feature.",
            "format": "double",
            "type": "number"
          },
          "project_id": {
            "type": "string"
          },
          "trigger": {
            "$ref": "#/$defs/PolicyTrigger"
          }
        },
        "required": [
          "id",
          "project_id",
          "trigger",
          "action"
        ],
        "title": "MarginPolicy",
        "type": "object"
      },
      "ModelPriceRow": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A persisted price-book row (the DB-backed source of truth; `pricing.json` is just the seed).\n\nThe book is **append-only and dated** (M26): a row's identity is\n`(provider, model, effective_from)`, so correcting a rate adds a row instead of overwriting the\none that priced last quarter's traffic, and `GET /v1/prices/history/:provider/:model` can show\nthe timeline. `verified_at` is when a human last checked the rate against the vendor's page —\nthe seed's `_meta.last_verified` used to be invisible at runtime, which is how a two-year-old\nprice book looks exactly like a freshly-checked one.",
        "properties": {
          "cached_input_per_mtok": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "effective_from": {
            "default": "2026-09-03T12:23:35.489883900Z",
            "description": "When this rate took effect. `effective_date` is accepted as the pre-M26 spelling, so a\nclient written against the old wire shape still parses.",
            "format": "date-time",
            "type": "string"
          },
          "input_per_mtok": {
            "format": "double",
            "type": "number"
          },
          "model": {
            "type": "string"
          },
          "note": {
            "description": "Free-text operator note — why the rate changed, a ticket id, a caveat.",
            "type": [
              "string",
              "null"
            ]
          },
          "output_per_mtok": {
            "format": "double",
            "type": "number"
          },
          "provider": {
            "type": "string"
          },
          "source_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "verified_at": {
            "description": "When this rate was last checked against the vendor's published pricing.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "provider",
          "model",
          "input_per_mtok",
          "output_per_mtok"
        ],
        "title": "ModelPriceRow",
        "type": "object"
      },
      "Project": {
        "$defs": {
          "Redaction": {
            "description": "How prompt/output payloads are persisted for a project.",
            "oneOf": [
              {
                "const": "none",
                "description": "Store payloads as sent.",
                "type": "string"
              },
              {
                "const": "hash",
                "description": "Store only a hash of payloads (presence/diff without content).",
                "type": "string"
              },
              {
                "const": "drop",
                "description": "Never persist payloads.",
                "type": "string"
              }
            ]
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A monitored application / tenant.",
        "properties": {
          "archived_at": {
            "description": "When the project was archived. `DELETE /v1/projects/:id` **archives** — it sets this and\n`enabled = false` — because a project's events, scores and benchmark runs are the record a\ncost report is built from; dropping the tenant row would orphan them silently.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "collective_opt_in": {
            "default": false,
            "description": "Consent to include this project's benchmark runs in a collective-network digest. Default\n**off**: contributing a project's eval results to a shared hub is an act, not an inheritance —\n`lt collective contribute` must never ship a project nobody opted in.",
            "type": "boolean"
          },
          "created_at": {
            "format": "date-time",
            "type": "string"
          },
          "enabled": {
            "default": true,
            "type": "boolean"
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "redaction": {
            "$ref": "#/$defs/Redaction",
            "default": "none"
          },
          "require_trusted_judge": {
            "default": false,
            "description": "Refuse to promote — a prompt label, a benchmark gate — on a judge that is not `trusted` for\nthe rubric being gated on (M11). Default **off**: turning it on retroactively would block\nevery existing deployment's gates on the day it upgraded, because nothing has been\ncalibrated yet. Once on, both `untrusted` and `unknown` block, because a gate that promotes\non a judge nobody has measured is precisely the failure this flag exists to close.",
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "name",
          "created_at"
        ],
        "title": "Project",
        "type": "object"
      },
      "Prompt": {
        "$defs": {
          "CanaryPolicy": {
            "description": "When a served canary version counts as having regressed against production, and what to do\nabout it.\n\nEvery default makes the policy *safe to enable*: `auto_revert` is off, so the first thing a\ncanary does is tell you rather than act; `min_n` is an evidence floor, because a version judged\nthree times has said nothing about whether it is worse; and `max_drop` is a **relative** band, so\nit means the same thing whatever scale the rubric scores on.",
            "properties": {
              "auto_revert": {
                "default": false,
                "description": "Move the canary's label back to the version it replaced when a regression is confirmed.\n**Off by default**: reverting changes what a deployment serves, and that is a decision.",
                "type": "boolean"
              },
              "label": {
                "default": "canary",
                "description": "The label carrying the version under test.",
                "type": "string"
              },
              "max_drop": {
                "default": 0.05,
                "description": "How far the canary's mean may fall below production's before it counts as a regression, as a\nfraction of production's mean (`0.05` = 5% worse).",
                "format": "double",
                "type": "number"
              },
              "min_n": {
                "default": 20,
                "description": "Verdicts required on **each** side before the comparison may decide anything. Below it the\nsweep stays silent — an evidence floor, not a threshold.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "production_label": {
                "default": "production",
                "description": "The label it is measured against.",
                "type": "string"
              },
              "window_secs": {
                "default": 86400,
                "description": "Trailing window the comparison reads, in seconds.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "LabelChange": {
            "description": "One label move: which label, to which version, when, and why.\n\n`reason` is free text with the two reserved spellings above, so a reader can separate \"someone\ndecided this\" from \"the canary decided this\" without parsing prose.",
            "properties": {
              "at": {
                "default": "2026-09-03T12:23:35.490690600Z",
                "format": "date-time",
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "version": {
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "label",
              "version"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A named entry in the prompt registry. Holds the label→version pointers (e.g.\n`{\"production\": 2, \"staging\": 5}`) fetched at runtime, plus an optional linked benchmark whose\nregression check gates promotion. The actual prompt text lives in [`PromptVersion`] rows, one per\nimmutable version — so a registry edit is a new version, never an in-place overwrite.",
        "properties": {
          "benchmark_id": {
            "description": "Benchmark run on each new version; its regression verdict gates label promotion. Reuses the\nexisting benchmark + job-queue machinery.",
            "type": [
              "string",
              "null"
            ]
          },
          "canary": {
            "anyOf": [
              {
                "$ref": "#/$defs/CanaryPolicy"
              },
              {
                "type": "null"
              }
            ],
            "description": "The online canary policy, when this prompt has one (M23). `None` leaves promotion as the last\nthing the registry ever observes about a version — the gap the canary closes."
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.490752800Z",
            "format": "date-time",
            "type": "string"
          },
          "id": {
            "default": "c940653f-d9c8-475f-a310-07d752cff1aa",
            "type": "string"
          },
          "label_history": {
            "description": "Every label move, append-only, oldest first, bounded to [`MAX_LABEL_HISTORY`].\n\nA label pointer alone answers \"what is served now\" and nothing else — not what it replaced,\nnot when, not why — so an automatic revert would be indistinguishable from a human\npromotion, and there would be no version for a revert to fall *back* to.",
            "items": {
              "$ref": "#/$defs/LabelChange"
            },
            "type": "array"
          },
          "labels": {
            "additionalProperties": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "default": {},
            "description": "Label → version pointers, e.g. `{\"production\": 3}`. A runtime fetch by `label` resolves here.",
            "type": "object"
          },
          "name": {
            "description": "Registry name, unique per project (e.g. `support-reply`).",
            "type": "string"
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "updated_at": {
            "default": "2026-09-03T12:23:35.490764500Z",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "Prompt",
        "type": "object"
      },
      "PromptVersion": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One immutable version of a [`Prompt`]. `version` is monotonic per prompt (1, 2, 3, …).",
        "properties": {
          "config": {
            "description": "Optional structured config (model, params, variable schema). Free-form."
          },
          "content": {
            "description": "The prompt text / template.",
            "type": "string"
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.491148900Z",
            "format": "date-time",
            "type": "string"
          },
          "id": {
            "default": "fb8c4b4f-93bf-4f00-81ba-aa49cd5254c0",
            "type": "string"
          },
          "note": {
            "description": "Optional change note (\"commit message\") describing why this version was cut.",
            "type": [
              "string",
              "null"
            ]
          },
          "prompt_id": {
            "default": "",
            "type": "string"
          },
          "version": {
            "default": 0,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "content"
        ],
        "title": "PromptVersion",
        "type": "object"
      },
      "RelayCancel": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "What a cancel request did to a relay task. Mirrors [`crate::JobCancel`] — the same three honest\nanswers, so the API never reports \"cancelled\" for a run that is still spending.",
        "oneOf": [
          {
            "description": "It was queued — nothing ever ran, and nothing ever will.",
            "properties": {
              "outcome": {
                "const": "cancelled",
                "type": "string"
              }
            },
            "required": [
              "outcome"
            ],
            "type": "object"
          },
          {
            "description": "It was leased: marked `cancelling`. The device learns at its next renewal, stops, and does\nnot deliver.",
            "properties": {
              "outcome": {
                "const": "cancelling",
                "type": "string"
              }
            },
            "required": [
              "outcome"
            ],
            "type": "object"
          },
          {
            "description": "It had already reached a terminal state; nothing changed.",
            "properties": {
              "outcome": {
                "const": "already_finished",
                "type": "string"
              },
              "status": {
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "status"
            ],
            "type": "object"
          }
        ],
        "title": "RelayCancel"
      },
      "RelayTask": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One queued unit of device work. `status` is one of [`RelayStatus::ALL`] (`queued` | `leased` |\n`succeeded` | `dead` | `cancelling` | `cancelled`). A failed attempt goes back to `queued` (with\n`error` recorded and `next_attempt_at` pushed out) until `max_attempts` is exhausted, which\nflips it to `dead`; a cancel moves a queued task to `cancelled` and a leased one through\n`cancelling`.",
        "properties": {
          "action_type": {
            "description": "The contract string resolved against the device's local action library,\ne.g. `xprice/reprice-summary`.",
            "type": "string"
          },
          "attempts": {
            "default": 0,
            "description": "How many times a device has **leased** this task. Bumped inside the atomic lease, so it\ncounts device deaths too — which is why it is no longer what decides a retry (see\n[`RelayTask::failures`]). A `Deferred` settle still hands one back, because a deferral is a\nlease that was never really used.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.491624Z",
            "format": "date-time",
            "type": "string"
          },
          "device": {
            "description": "Which device holds (or last held) the lease.",
            "type": [
              "string",
              "null"
            ]
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "failures": {
            "default": 0,
            "description": "How many times the task actually RAN and failed (the device reported a failure). This — not\n`attempts` — is the retry budget measured against `max_attempts`, so a device killed\nmid-run no longer burns one of the task's chances.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "id": {
            "default": "137d2a77-f060-4bc2-ac76-2f71602b69da",
            "type": "string"
          },
          "idempotency_key": {
            "description": "Client-supplied dedupe key, unique per project — re-enqueueing returns the existing task.",
            "type": [
              "string",
              "null"
            ]
          },
          "lease_deadline": {
            "description": "A lease past this deadline is reclaimable (re-leased, or dead if attempts are exhausted).",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "lease_fence": {
            "description": "The holding device's own fencing token: the instant its lease was granted, compared for\nexact equality on every write it makes about this task.\n\nWithout it, `settle` could only ask \"is this task still leased?\" — which a task re-leased to\na *second* device answers yes to, so the first device's late report landed on the second\ndevice's run. Renewal moves `lease_deadline`, never this: one device's lease keeps one\nidentity for its whole run.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "max_attempts": {
            "default": 4,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "next_attempt_at": {
            "default": "2026-09-03T12:23:35.491595900Z",
            "description": "The task is not leasable before this instant (retry backoff).",
            "format": "date-time",
            "type": "string"
          },
          "payload": {
            "description": "JSON parameters substituted into the action's local prompt template."
          },
          "progress": {
            "description": "Free-text liveness detail the device publishes while running, visible in `get_relay_task`.\nCarried on `/progress`, never on the renewal — liveness must never wait on the work having\nsomething to say, or a live-but-stuck device reads as a dead one.",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "type": "string"
          },
          "result": true,
          "retry_interval_secs": {
            "default": 18000,
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "source": {
            "description": "Free-form originator tag (which app/service enqueued it).",
            "type": [
              "string",
              "null"
            ]
          },
          "stale_reclaims": {
            "default": 0,
            "description": "How many times this task was reclaimed from a device that held it past its lease deadline\nwithout finishing: the count of *device deaths*, kept apart from `failures` so an operator\ncan tell \"the action keeps failing\" from \"the laptop keeps sleeping\". Dead-letters at\n[`RELAY_MAX_STALE_RECLAIMS`].",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "status": {
            "default": "queued",
            "type": "string"
          },
          "updated_at": {
            "default": "2026-09-03T12:23:35.491637600Z",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "project_id",
          "action_type"
        ],
        "title": "RelayTask",
        "type": "object"
      },
      "RevenueEvent": {
        "$defs": {
          "RevenueKind": {
            "description": "How a revenue record is recognized. `amount_usd` is always a non-negative magnitude; `Refund`\nflips its sign at recognition time, so refunds/credits reduce recognized revenue.",
            "oneOf": [
              {
                "const": "subscription",
                "description": "Recurring subscription; amortized across `[period_start, period_end]`.",
                "type": "string"
              },
              {
                "const": "one_time",
                "description": "One-off charge recognized at `ts`.",
                "type": "string"
              },
              {
                "const": "usage",
                "description": "Usage-based charge recognized at `ts`.",
                "type": "string"
              },
              {
                "const": "refund",
                "description": "Refund/credit — subtracts from recognized revenue.",
                "type": "string"
              }
            ]
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One normalized revenue record — the revenue analog of [`crate::LlmEvent`]'s cost. Synced from a\nbilling provider (Stripe/Polar) or posted by hand; `external_id` is the provider's own id, used for\nidempotent upserts. Attributed to a customer and/or product so it can be netted against LLM cost.",
        "properties": {
          "amount_minor": {
            "description": "The provider's own figure, in the currency's **minor unit** (Stripe `amount_paid`, Polar\n`total_amount`). The one number on this row that never needs restating: `amount_usd` is a\nderived value and a wrong rate makes it wrong, but ¥5000 was ¥5000 whatever the book said.\nKeeping it is what makes [`crate::RevenueEvent`] repriceable instead of re-ingestible.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "amount_usd": {
            "description": "Non-negative magnitude in USD; sign is derived from `kind` at recognition time.",
            "format": "double",
            "type": "number"
          },
          "converted": {
            "description": "Whether a real conversion happened. `Some(false)` is the **1:1 fallback**: no rate existed,\nso the major-unit figure was stored as if it were USD and is approximate. `None` is a row\nthat predates the field — read it through [`RevenueEvent::is_converted`], never as `false`.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "currency": {
            "default": "USD",
            "type": "string"
          },
          "customer_id": {
            "description": "Billing customer this revenue is attributed to (joins to events' `metadata.customer_id`).",
            "type": [
              "string",
              "null"
            ]
          },
          "external_id": {
            "description": "The provider's own id for this record (invoice/charge/order) — for idempotent upsert.",
            "type": [
              "string",
              "null"
            ]
          },
          "fx_book_version": {
            "description": "Which FX book produced `fx_rate` ([`crate::revenue`]'s caller passes\n`lighttrack_billing::FxTable::version`). Without it, \"we fixed the EUR rate\" and \"these rows\nalready had the fixed rate\" are the same sentence.",
            "type": [
              "string",
              "null"
            ]
          },
          "fx_rate": {
            "description": "USD per one major unit of `currency` at the time of conversion. `1.0` for the base currency;\n`None` on a row written before FX provenance existed, or on the 1:1 fallback.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "id": {
            "default": "cb184ccf-8d36-4e04-afb7-4716f4b6d504",
            "type": "string"
          },
          "kind": {
            "$ref": "#/$defs/RevenueKind",
            "default": "one_time"
          },
          "period_end": {
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "period_start": {
            "description": "Recognition window for subscriptions; if unset the full amount is recognized at `ts`.",
            "format": "date-time",
            "type": [
              "string",
              "null"
            ]
          },
          "product_id": {
            "description": "Billing product/feature this revenue is attributed to (joins to events' `metadata.product_id`).",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "source": {
            "default": "manual",
            "description": "Source billing system, e.g. `stripe` | `polar` | `manual`.",
            "type": "string"
          },
          "ts": {
            "default": "2026-09-03T12:23:35.492010800Z",
            "format": "date-time",
            "type": "string"
          }
        },
        "required": [
          "amount_usd"
        ],
        "title": "RevenueEvent",
        "type": "object"
      },
      "Rubric": {
        "$defs": {
          "DimensionCheck": {
            "description": "Per-kind configuration for a deterministic dimension. Every field is optional, so one struct\nserves all kinds and an `llm` dimension serializes without it at all.",
            "properties": {
              "case_sensitive": {
                "default": false,
                "description": "Compare case-sensitively (default: false — mechanical checks shouldn't fail on casing).",
                "type": "boolean"
              },
              "expect": {
                "description": "The literal target for `exact` / `contains` / `numeric` (and, optionally, `json_valid`).\nDefaults to the case's `expected` reference answer when unset.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "path": {
                "description": "JSON Pointer (RFC 6901, e.g. `/data/answer`) selecting the part of a JSON output to check.\nUnset = check the whole output.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "pattern": {
                "description": "`regex` only: the pattern the output must match somewhere (unanchored).",
                "type": [
                  "string",
                  "null"
                ]
              },
              "tolerance": {
                "description": "`numeric` only: absolute tolerance around the target. Unset = exact equality.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "trim": {
                "default": true,
                "description": "Trim surrounding whitespace from both sides before comparing (default: true).",
                "type": "boolean"
              }
            },
            "type": "object"
          },
          "DimensionKind": {
            "description": "How a dimension is evaluated. `Llm` (the default) asks the judge model; every other kind is a\nmechanical check the engine runs locally at zero tokens and zero cost, scored into the same\nweighting / floor / aggregation pipeline. Additive and defaulted: a rubric written before kinds\nexisted deserializes as all-`Llm` and re-serializes byte-identically.",
            "oneOf": [
              {
                "const": "llm",
                "description": "Scored by the judge model against the dimension's description and anchors.",
                "type": "string"
              },
              {
                "const": "exact",
                "description": "The output must equal the target exactly (after the configured trim/case handling).",
                "type": "string"
              },
              {
                "const": "regex",
                "description": "The output must match `check.pattern` (unanchored regex).",
                "type": "string"
              },
              {
                "const": "numeric",
                "description": "The output's number must be within `check.tolerance` of the target.",
                "type": "string"
              },
              {
                "const": "json_valid",
                "description": "The output must parse as JSON (and, with `check.expect`, carry that value at `check.path`).",
                "type": "string"
              },
              {
                "const": "contains",
                "description": "The output must contain the target as a substring.",
                "type": "string"
              }
            ]
          },
          "RubricDimension": {
            "description": "One scored dimension of a rubric (e.g. correctness, completeness, faithfulness, concision).",
            "properties": {
              "anchors": {
                "description": "Anchored level descriptions, e.g. [\"1.0 = fully correct & verifiable\", \"0.5 = minor error\", \"0 = wrong\"].",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "check": {
                "$ref": "#/$defs/DimensionCheck",
                "description": "Configuration for a deterministic `kind`. Ignored when `kind` is `llm`."
              },
              "description": {
                "description": "What this dimension measures.",
                "type": "string"
              },
              "floor": {
                "description": "Gating floor: if this dimension scores below it, the case fails regardless of the overall.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "key": {
                "description": "Stable key used in the judge's JSON output (must be a valid identifier-ish string).",
                "type": "string"
              },
              "kind": {
                "$ref": "#/$defs/DimensionKind",
                "description": "How this dimension is evaluated (default `llm`). Deterministic kinds are checked locally and\nare never narrated to the judge model, so they cannot be double-counted."
              },
              "weight": {
                "default": 1.0,
                "description": "Relative weight in the overall score.",
                "format": "double",
                "type": "number"
              }
            },
            "required": [
              "key",
              "description"
            ],
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A weighted, anchored rubric — the judge's scoring contract (see docs/BENCHMARK_FRAMEWORK.md §3).",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.492392100Z",
            "format": "date-time",
            "type": "string"
          },
          "dimensions": {
            "items": {
              "$ref": "#/$defs/RubricDimension"
            },
            "type": "array"
          },
          "id": {
            "default": "9b09a2fc-ca64-4cfb-ab27-8226d40f5641",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "project_id": {
            "default": "",
            "type": "string"
          },
          "supersedes": {
            "description": "The rubric id this one replaces, so the chain is walkable in both directions. `None` on the\nfirst version of a rubric.",
            "type": [
              "string",
              "null"
            ]
          },
          "threshold": {
            "default": 0.7,
            "description": "Overall pass threshold (weighted score, 0–1).",
            "format": "double",
            "type": "number"
          },
          "version": {
            "description": "Which generation of this rubric this is. Editing a rubric changes what a score *means*, so\ncomparing verdicts across an edit is comparing two different measurements — and nothing\nrecorded that an edit had happened. Starts at 1; `POST /v1/rubrics/:id/versions` mints the\nnext.\n\nA new version is a **new row with a new id**, not a mutation: the old rubric must stay\nreadable, because the verdicts that cite it are still stored and still cite it.\n\nOmitted from the wire at generation 1, which is the same statement as absence (absent\ndeserializes to 1) and keeps a pre-versioning rubric serializing byte-identically.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "name",
          "dimensions"
        ],
        "title": "Rubric",
        "type": "object"
      },
      "Schedule": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "One recurring workload.",
        "properties": {
          "created_at": {
            "default": "2026-09-03T12:23:35.492709800Z",
            "format": "date-time",
            "type": "string"
          },
          "enabled": {
            "default": true,
            "type": "boolean"
          },
          "id": {
            "default": "41364089-772d-4221-834d-466b3fc5817c",
            "type": "string"
          },
          "interval_secs": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "kind": {
            "description": "The [`JobKind`](crate::JobKind) wire literal this schedule enqueues (`bench_run`, …). A\n`String` on the row for the same reason `Job::job_type` is: a row written by a newer version\ndeserializes on an older one instead of hard-failing the whole list.",
            "type": "string"
          },
          "last_job_id": {
            "description": "The most recent job this schedule produced — the link `GET /v1/schedules/:id/runs` follows.",
            "type": [
              "string",
              "null"
            ]
          },
          "next_due": {
            "description": "When the sweep should next enqueue for this schedule.",
            "format": "date-time",
            "type": "string"
          },
          "payload": {
            "description": "The job payload, enqueued verbatim. Validated against `kind` when the schedule is written."
          },
          "project_id": {
            "type": "string"
          }
        },
        "required": [
          "project_id",
          "kind",
          "interval_secs",
          "next_due"
        ],
        "title": "Schedule",
        "type": "object"
      },
      "Score": {
        "$defs": {
          "ScoreDetail": {
            "description": "Structured provenance for a judged verdict: the per-dimension breakdown plus the reliability\nsignals (agreement, sample accounting, bias/injection flags) that produced the scalar `value` on\n[`Score`]. Additive and nullable — a score posted without it is still a valid score.",
            "properties": {
              "agreement": {
                "description": "Cross-sample agreement on the overall score (1.0 = the samples agreed exactly).",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "coverage": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/TraceCoverage"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "For a **whole-trace** verdict: what it judged (span count + the fingerprint of the judged\nroot exchange). Stamped by the API on `POST /v1/traces/:id/score`; a trace read compares it\nagainst the trace as it now stands so a verdict that stopped describing its trace says so\ninstead of aging silently. `None` on every non-trace score."
              },
              "determinism": {
                "description": "`\"exact\"` when every sampling control the judge provider exposes was pinned (temperature 0 +\na fixed seed); `\"best-effort\"` when the path exposed no seed, no knobs at all, or rejected\nthe ones we asked for. On a best-effort verdict, cross-sample agreement partly measures\nsampling noise rather than genuine ambiguity — so the number means less.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "dimensions": {
                "items": {
                  "$ref": "#/$defs/ScoreDim"
                },
                "type": "array"
              },
              "evidence_redacted_spans": {
                "description": "How many PII spans the ingest scrub had already replaced in the evidence this verdict was\ncomputed from ([`crate::LlmEvent::redaction`]'s `spans`, copied at judge time).\n\nThe judge reads the *stored* text, so a scrub that mangled a payload silently changes what\nwas judged — D14's un-observable defect, now observable at the verdict rather than only at\nthe row. `Some(0)` is a scrubbed-and-untouched payload; `None` is a verdict whose event\ncarried no stamp at all, which is a weaker statement and kept distinct from it.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "injection_suspected": {
                "description": "The judged content imitated a prompt boundary and was neutralized (see engine `fence`).",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "notes": {
                "description": "Judge text not tied to a dimension (a freeform verdict's rationale, a pairwise rationale).",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "parse_failures": {
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "position_bias": {
                "description": "Pairwise only: swapping A/B flipped the winner, so the verdict collapsed to a tie.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "samples_parsed": {
                "description": "How many of the requested samples yielded a usable verdict.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "samples_requested": {
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "type": "object"
          },
          "ScoreDim": {
            "description": "One dimension's contribution to a judged verdict, kept so a stored score can answer *why*.",
            "properties": {
              "floor": {
                "description": "The rubric's gating floor for this dimension, when it has one.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "floor_hit": {
                "default": false,
                "description": "True when `value` fell below `floor` — the reason a high overall can still fail. On a\nmerged cell this is the OR across the cells merged, so it can be true while the merged\n(mean) `value` clears the floor; read `floor_hits`/`floor_of` for the shape.",
                "type": "boolean"
              },
              "floor_hits": {
                "description": "How many contributing observations fell below `floor`, and how many there were. One\njudged verdict reports its own; a merged cell reports the tally across what it merged.\n\n`floor_hit` alone cannot separate a dimension that crossed on **every** observation from\none that crossed on **one of five**, because an OR reduces both to `true` — and that is\nexactly the difference between a boundary the candidate always hits and one it sometimes\ndoes. The count carries its own denominator so the two are never confused.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "floor_of": {
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "key": {
                "type": "string"
              },
              "reasoning": {
                "description": "The judge's reasoning, one entry per sample that parsed (sample order). Every sample is kept:\nits reasoning tokens were paid for.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "value": {
                "description": "The aggregated (mean-over-samples) dimension score.",
                "format": "double",
                "type": "number"
              },
              "weight": {
                "format": "double",
                "type": "number"
              }
            },
            "required": [
              "key",
              "value",
              "weight"
            ],
            "type": "object"
          },
          "ScoreKind": {
            "description": "What kind of verdict a [`Score`] is.\n\n`Score.rubric` is one free-text column carrying six different encodings — a bare rubric name,\n`bench:{name}`, `{name}:{label}#case{i}`, a pairwise label, `lt:calibration:…`, a trace rubric —\nso every consumer had to parse a string to find out what it was reading, and the alerting path\nkeyed its window on that string. A compare cell therefore minted a **unique key per case**, and a\nwindow that never sees the same key twice never accumulates: `score_drop` could not fire on a\nbenchmark's case stream at all.\n\nTyped here, defaulted to [`ScoreKind::Freeform`], and the legacy `rubric` string is kept verbatim\nbeside it — this classifies existing verdicts rather than replacing their identity.",
            "oneOf": [
              {
                "const": "freeform",
                "description": "An ad-hoc verdict with no rubric behind it (the online scorer's default judge).",
                "type": "string"
              },
              {
                "const": "rubric",
                "description": "Judged against a stored [`crate::Rubric`]; `rubric_id` names it.",
                "type": "string"
              },
              {
                "const": "bench_case",
                "description": "One case of a benchmark run.",
                "type": "string"
              },
              {
                "const": "compare_cell",
                "description": "One (target × case) cell of a comparison matrix.",
                "type": "string"
              },
              {
                "const": "pairwise_game",
                "description": "One game of a pairwise comparison.",
                "type": "string"
              },
              {
                "const": "calibration",
                "description": "A judge-calibration probe (`lt:calibration:…`), not a product verdict.",
                "type": "string"
              },
              {
                "const": "trace",
                "description": "A whole-trace verdict, anchored to the trace's root span.",
                "type": "string"
              },
              {
                "const": "other",
                "description": "A kind this binary does not know — a newer writer's verdict read by an older reader. Kept as\na variant rather than silently folded into `Freeform`, which would misfile it.",
                "type": "string"
              }
            ]
          },
          "TraceCoverage": {
            "description": "What a whole-trace verdict actually judged, recorded on the verdict itself.\n\nA trace has no end marker: a span that lands after the judge ran is folded straight into the next\nread (new totals, a new node in the tree) while the verdict stays put and silently stops\ndescribing the trace. This is the receipt that makes that detectable — the trace's size and the\nfingerprint of the exchange the judge actually read, so a later read can compare and say so.\n\nThe digest covers the **root exchange** (the root span's id + input + output) rather than every\nspan, deliberately:\n- it is what a whole-trace judge is handed, so it changes exactly when the judged text changes;\n- it survives the [`Trace::spans_truncated`] cap — the detail read keeps the *oldest* spans, so\n  the root is always present and a clipped trace fingerprints identically to the whole one. A\n  truncated trace must never be mistaken for a changed one.",
            "properties": {
              "digest": {
                "description": "Fingerprint of that exchange. Changes iff the text the judge saw changed.",
                "type": "string"
              },
              "root_event_id": {
                "description": "The event whose exchange was judged (the trace's entry-point span).",
                "type": "string"
              },
              "spans": {
                "default": 0,
                "description": "The trace's true span count when judged — `spans_total`, so a clipped read still records the\nreal number.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              },
              "truncated": {
                "description": "The read this verdict was formed over was clipped by the span cap — provenance only, never a\ndrift signal.",
                "type": "boolean"
              }
            },
            "type": "object"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "A stored judge result, optionally tied to the event it scored.",
        "properties": {
          "case_index": {
            "description": "1-based position of the case within the run's dataset (pairwise: the case a game was played\non, so it repeats across that case's games). Ordering key for the run's case listing.",
            "format": "uint32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cost_usd": {
            "description": "Cost of the judge call. Recorded for visibility (Agent SDK credit burn); never throttled.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "created_at": {
            "default": "2026-09-03T12:23:35.494848400Z",
            "format": "date-time",
            "type": "string"
          },
          "detail": {
            "anyOf": [
              {
                "$ref": "#/$defs/ScoreDetail"
              },
              {
                "type": "null"
              }
            ],
            "description": "Structured provenance for this verdict (per-dimension breakdown, agreement, sample\naccounting, bias flags). Additive and nullable: pre-existing clients that never send or read\nit keep working. Persisted by the SQLite backend; other backends default it to `None`."
          },
          "event_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "default": "3b0e5460-226d-4789-ae8e-41eb86e1a7a7",
            "type": "string"
          },
          "kind": {
            "$ref": "#/$defs/ScoreKind",
            "default": "freeform",
            "description": "What sort of verdict this is. Defaults to [`ScoreKind::Freeform`] so every stored score\ndeserializes; the runner stamps the real kind at every producer."
          },
          "max": {
            "default": 1.0,
            "format": "double",
            "type": "number"
          },
          "pass": {
            "type": [
              "boolean",
              "null"
            ]
          },
          "project_id": {
            "default": "",
            "description": "Defaulted so a keyed poster can omit it (the API derives it from the API key).",
            "type": "string"
          },
          "reasoning": {
            "type": [
              "string",
              "null"
            ]
          },
          "rubric": {
            "description": "The verdict's **human-readable label**, kept verbatim for every existing consumer: a bare\nrubric name, `bench:{name}`, `{name}:{label}#case{i}`, `lt:calibration:…`. Six encodings in\none column, which is why it is no longer the identity — [`Score::rubric_id`] and\n[`Score::kind`] are.",
            "type": "string"
          },
          "rubric_id": {
            "description": "The [`crate::Rubric`] this verdict was judged against, when there was one. `None` for a\nfreeform verdict, and for a verdict written before the column existed.\n\nThis is the join the label could never be: a rubric renamed between two runs used to make\nthem two unrelated series, and two rubrics that happened to share a name used to look like\none.",
            "type": [
              "string",
              "null"
            ]
          },
          "run_id": {
            "description": "The [`BenchmarkRun`] this verdict belongs to, when it came from a benchmark. Without it, two\nruns of the same benchmark are distinguishable only by `created_at` ordering, so \"why did run\n47 fail?\" has no query. The runner mints the run id **before** judging and stamps it on every\ncase it posts, so the run's cases exist even if the run row is never written.",
            "type": [
              "string",
              "null"
            ]
          },
          "scored_by": {
            "description": "Judge model, e.g. `claude-haiku-4-5`.",
            "type": "string"
          },
          "value": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "rubric",
          "value",
          "scored_by"
        ],
        "title": "Score",
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "description": "An admin key, or a project key carrying the capability the operation declares (`ingest`, `read`, `manage`). Sent as `Authorization: Bearer …`.",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "Self-hosted LLM observability, LLM-as-judge scoring and benchmarking. This document is generated from `lighttrack-contract`, the single table the axum router, the MCP tool catalog, the `lt` CLI and the Markdown renderer are all held to by test.",
    "title": "LightTrack API",
    "version": "0.0.1"
  },
  "openapi": "3.1.0",
  "paths": {
    "/health": {
      "get": {
        "description": "Liveness, and the store backend's declared surfaces.\n\nNo LightTrack principal — authenticated by the caller's own signature, or not at all.",
        "operationId": "health",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ status, backend, surfaces: [name] } — liveness plus the store backend's declared surfaces.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "security": [],
        "summary": "Liveness, and the store backend's declared surfaces."
      }
    },
    "/openapi.json": {
      "get": {
        "description": "This deployment's OpenAPI 3.1 description, generated from the endpoint contract.\n\nNo LightTrack principal — authenticated by the caller's own signature, or not at all.",
        "operationId": "openapi",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "An OpenAPI 3.1 document generated from this contract: every path, its parameters, its request and response schemas, and the capability each operation requires.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "security": [],
        "summary": "This deployment's OpenAPI 3.1 description, generated from the endpoint contract."
      }
    },
    "/v1/alert-channels/{id}/test": {
      "post": {
        "description": "Send a real, signed test alert down one channel and report whether it landed.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "test_alert_channel",
        "parameters": [
          {
            "description": "alert channel id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "alert channel id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ channel_id, target, signed, ok, status } — what a real test delivery down this channel actually did.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Send a real, signed test alert down one channel and report whether it landed."
      }
    },
    "/v1/alerts": {
      "get": {
        "description": "The fired-alert ledger, newest first: deliveries, acknowledgement and resolution.\n\nAn admin, or a project key carrying the `read` capability.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "list_alerts",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "only alerts of this kind",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "description": "only alerts of this kind",
              "enum": [
                "limit_breach",
                "limit_warning",
                "forecast_alert",
                "relay_task_dead",
                "error_spike",
                "score_drop",
                "bench_run",
                "ingest_rejected"
              ],
              "type": "string"
            }
          },
          {
            "description": "window start: an RFC3339 instant, or a relative 30m / 24h / 7d",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "window start: an RFC3339 instant, or a relative 30m / 24h / 7d",
              "type": "string"
            }
          },
          {
            "description": "true = only acknowledged, false = only open (the on-call view); omit for both",
            "in": "query",
            "name": "acked",
            "required": false,
            "schema": {
              "description": "true = only acknowledged, false = only open (the on-call view); omit for both",
              "type": "boolean"
            }
          },
          {
            "description": "max alerts (default 20)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max alerts (default 20)",
              "type": "integer"
            }
          },
          {
            "description": "keyset cursor from a prior call's next_cursor",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "keyset cursor from a prior call's next_cursor",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ alerts: [Alert], next_cursor } — the fired-alert ledger newest first, each row carrying its deliveries, acknowledgement and resolution.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The fired-alert ledger, newest first: deliveries, acknowledgement and resolution."
      }
    },
    "/v1/alerts/{id}/ack": {
      "post": {
        "description": "Acknowledge one alert: record that someone saw it.\n\nAn admin, or a project key carrying the `manage` capability.",
        "operationId": "ack_alert",
        "parameters": [
          {
            "description": "the alert id from list_alerts",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "the alert id from list_alerts",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "by": {
                    "description": "who saw it — an on-call handle, an email, a runbook link. Defaults server-side to the calling key's label.",
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ acked, acked_at } — the alert id and the instant it was seen.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Acknowledge one alert: record that someone saw it."
      }
    },
    "/v1/alerts/{id}/resolution": {
      "post": {
        "description": "Attach what came of an alert, turning a notification into a closed loop.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "attach_alert_resolution",
        "parameters": [
          {
            "description": "the alert id being closed out",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "the alert id being closed out",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": true,
                "description": "Any JSON object — the responder's diagnosis, stored verbatim on the alert.",
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ resolved } — the alert id the resolution was attached to.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Attach what came of an alert, turning a notification into a closed loop."
      }
    },
    "/v1/benchmark-runs": {
      "post": {
        "description": "Record a finished benchmark run's scorecard; fires the run-completion alert.\n\nAn admin, or a project key carrying the `manage` capability.",
        "operationId": "post_benchmark_run",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BenchmarkRun"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BenchmarkRun"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record a finished benchmark run's scorecard; fires the run-completion alert."
      }
    },
    "/v1/benchmarks/{id}": {
      "get": {
        "description": "One benchmark by id, scoped to the caller's project.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_benchmark",
        "parameters": [
          {
            "description": "benchmark id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "benchmark id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Benchmark"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One benchmark by id, scoped to the caller's project."
      }
    },
    "/v1/benchmarks/{id}/enqueue": {
      "post": {
        "description": "Queue a `bench_run` job for this benchmark; a runner executes it out of band.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "enqueue_benchmark",
        "parameters": [
          {
            "description": "benchmark id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "benchmark id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "heal": {
                    "description": "attempt prompt healing on low scores (default false)",
                    "type": "boolean"
                  },
                  "samples": {
                    "description": "runs per case (default 1)",
                    "type": "integer"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Job"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Queue a `bench_run` job for this benchmark; a runner executes it out of band."
      }
    },
    "/v1/benchmarks/{id}/gate": {
      "get": {
        "description": "CI-gate verdict from the latest finished run; 409 when policy requires a trusted judge.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "benchmark_gate",
        "parameters": [
          {
            "description": "benchmark id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "benchmark id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ status: pass|regressed|no_baseline|no_runs|partial, run_id?, mean?, baseline?, n?, judge_trust? } — the verdict from the latest FINISHED run, plus whether the judge behind it has ever been checked against a human.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "CI-gate verdict from the latest finished run; 409 when policy requires a trusted judge."
      }
    },
    "/v1/benchmarks/{id}/runs": {
      "get": {
        "description": "A benchmark's run history, newest first.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_benchmark_runs",
        "parameters": [
          {
            "description": "benchmark id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "benchmark id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/BenchmarkRun"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A benchmark's run history, newest first."
      }
    },
    "/v1/billing/{provider}/webhook": {
      "post": {
        "description": "Signed Stripe/Polar webhook door: a verified delivery becomes revenue in one atomic batch.\n\nNo LightTrack principal — authenticated by the caller's own signature, or not at all.",
        "operationId": "billing_webhook",
        "parameters": [
          {
            "description": "configured billing provider, e.g. `stripe` or `polar`",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "description": "configured billing provider, e.g. `stripe` or `polar`",
              "type": "string"
            }
          },
          {
            "description": "the LightTrack project the revenue lands in; configure one endpoint per project",
            "in": "query",
            "name": "project",
            "required": true,
            "schema": {
              "description": "the LightTrack project the revenue lands in; configure one endpoint per project",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "No content."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "security": [],
        "summary": "Signed Stripe/Polar webhook door: a verified delivery becomes revenue in one atomic batch."
      }
    },
    "/v1/calibrations": {
      "get": {
        "description": "Judge-human calibration history, newest first.\n\nAn admin, or a project key carrying the `read` capability.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "list_calibrations",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "integer"
            }
          },
          {
            "description": "opaque keyset cursor from a previous page",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "opaque keyset cursor from a previous page",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ calibrations: [CalibrationRecord], next_cursor } — κ, Pearson, MAE, RMSE and n per measurement, newest first.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Judge-human calibration history, newest first."
      },
      "post": {
        "description": "Record a completed judge-human calibration — the row a trust verdict is decided from.\n\nAn admin, or a project key carrying the `manage` capability.",
        "operationId": "post_calibration",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CalibrationRecord"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalibrationRecord"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record a completed judge-human calibration — the row a trust verdict is decided from."
      }
    },
    "/v1/capabilities": {
      "get": {
        "description": "What this deployment's store backend serves, and what it answers 501 for.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_capabilities",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ backend, surfaces: {name: bool}, refuses: [name], atomic_limits: bool } — what this deployment's store backend implements and what it answers 501 `unsupported` for.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "What this deployment's store backend serves, and what it answers 501 for."
      }
    },
    "/v1/collective/contribute": {
      "post": {
        "description": "Build this instance's digest, hash-gate it, push it to a hub, and record the attempt.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "post_contribute",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "force": {
                    "description": "push even when the digest is unchanged since the last ack",
                    "type": "boolean"
                  },
                  "hub": {
                    "description": "the hub's absolute http(s) base URL",
                    "type": "string"
                  },
                  "hub_key_ref": {
                    "description": "the NAME of a server-side env var holding the hub key — never the key itself",
                    "type": "string"
                  },
                  "min_cases": {
                    "description": "k-anonymity floor for the digest being built",
                    "type": "integer"
                  }
                },
                "required": [
                  "hub"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ outcome: sent|rejected|failed|skipped, hub_url_hash, entries, projects_included, projects_excluded, digest_sha256, contribution_id?, ack?, reason? } — a `skipped` push writes no ledger row, because nothing left the building.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Build this instance's digest, hash-gate it, push it to a hub, and record the attempt."
      }
    },
    "/v1/collective/contribution": {
      "delete": {
        "description": "Withdraw a source's contributed entries — consent stays revocable, not one-way.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "withdraw_contribution",
        "parameters": [
          {
            "description": "admin-only: withdraw a NAMED source (the one that lost its key)",
            "in": "query",
            "name": "contributor",
            "required": false,
            "schema": {
              "description": "admin-only: withdraw a NAMED source (the one that lost its key)",
              "type": "string"
            }
          },
          {
            "description": "flip the route around: withdraw what WE sent to every ledgered hub (admin)",
            "in": "query",
            "name": "all",
            "required": false,
            "schema": {
              "description": "flip the route around: withdraw what WE sent to every ledgered hub (admin)",
              "type": "string"
            }
          },
          {
            "description": "with `all`: a hub base URL to consider, repeatable",
            "in": "query",
            "name": "hub",
            "required": false,
            "schema": {
              "description": "with `all`: a hub base URL to consider, repeatable",
              "type": "string"
            }
          },
          {
            "description": "with `all`: the NAME of the env var holding the hub key, never the key",
            "in": "query",
            "name": "hub_key_ref",
            "required": false,
            "schema": {
              "description": "with `all`: the NAME of the env var holding the hub key, never the key",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ contributor_id, deleted } for the hub-side self-delete; with `all=1`, the contributor-side fan-out's per-hub report instead.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Withdraw a source's contributed entries — consent stays revocable, not one-way."
      }
    },
    "/v1/collective/contributions": {
      "get": {
        "description": "The contributor-side ledger: what we pushed, to which hub, and what it said back.\n\nAdmin (or dev-mode) principals only.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "get_contributions",
        "parameters": [
          {
            "description": "max rows, newest first (default from server)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max rows, newest first (default from server)",
              "type": "integer"
            }
          },
          {
            "description": "keyset cursor from a prior page's X-Next-Cursor",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "keyset cursor from a prior page's X-Next-Cursor",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ContributionRecord"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The contributor-side ledger: what we pushed, to which hub, and what it said back."
      }
    },
    "/v1/collective/digest": {
      "get": {
        "description": "Preview this instance's contributable digest, built only from consenting projects' runs.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_collective_digest",
        "parameters": [
          {
            "description": "k-anonymity floor: only (model, task) buckets with ≥ this many cases are published",
            "in": "query",
            "name": "min_cases",
            "required": false,
            "schema": {
              "description": "k-anonymity floor: only (model, task) buckets with ≥ this many cases are published",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CollectiveDigest"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Preview this instance's contributable digest, built only from consenting projects' runs."
      }
    },
    "/v1/collective/ingest": {
      "post": {
        "description": "Hub door: accept a contributor's digest and replace that source's stored entry set.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "post_collective_ingest",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CollectiveDigest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ contributor_id, accepted, skipped, dropped_under_min, rejected_implausible } — the identity is the HUB's (derived from the credential, never from the body), and every drop is disclosed back rather than silently absorbed.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Hub door: accept a contributor's digest and replace that source's stored entry set."
      }
    },
    "/v1/collective/leaderboard": {
      "get": {
        "description": "The merged cross-contributor model leaderboard, k-anonymized over sources.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_collective_leaderboard",
        "parameters": [
          {
            "description": "filter to one task bucket (qa, summarization, coding, …)",
            "in": "query",
            "name": "task_type",
            "required": false,
            "schema": {
              "description": "filter to one task bucket (qa, summarization, coding, …)",
              "type": "string"
            }
          },
          {
            "description": "filter to one provider (anthropic, openai, …)",
            "in": "query",
            "name": "provider",
            "required": false,
            "schema": {
              "description": "filter to one provider (anthropic, openai, …)",
              "type": "string"
            }
          },
          {
            "description": "keep rows scored (at least partly) by one judge family",
            "in": "query",
            "name": "judge",
            "required": false,
            "schema": {
              "description": "keep rows scored (at least partly) by one judge family",
              "type": "string"
            }
          },
          {
            "description": "rigor filter: every source ran at this level (exact|best-effort|sampled)",
            "in": "query",
            "name": "determinism",
            "required": false,
            "schema": {
              "description": "rigor filter: every source ran at this level (exact|best-effort|sampled)",
              "type": "string"
            }
          },
          {
            "description": "rigor filter: 'true' keeps only rows whose every source ran against a frozen, single-version dataset",
            "in": "query",
            "name": "frozen_dataset",
            "required": false,
            "schema": {
              "description": "rigor filter: 'true' keeps only rows whose every source ran against a frozen, single-version dataset",
              "type": "string"
            }
          },
          {
            "description": "rigor filter: 'true' keeps only rows whose every source's verdict was significance-tested",
            "in": "query",
            "name": "significance_tested",
            "required": false,
            "schema": {
              "description": "rigor filter: 'true' keeps only rows whose every source's verdict was significance-tested",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ contributors, n_models, n_rows, held_back, task_type?, rows: [LeaderboardRow] } — counts are computed over the FILTERED rows, and `held_back` discloses the rows withheld for having fewer than the hub's `min_contributors` distinct sources.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The merged cross-contributor model leaderboard, k-anonymized over sources."
      }
    },
    "/v1/costs": {
      "get": {
        "description": "Cost and usage grouped by project, provider and model over an optional window.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_costs",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (inclusive); omit for full history",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (inclusive); omit for full history",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (exclusive)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (exclusive)",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[{ project_id, provider, model, calls, input_tokens, output_tokens, cost_usd, unpriced_calls }] — cost grouped by project×provider×model. The price book's freshness rides in `x-price-book-verified-at` / `x-price-book-stale` because the body is a bare array the render layer is written against.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Cost and usage grouped by project, provider and model over an optional window."
      }
    },
    "/v1/costs/prompts": {
      "get": {
        "description": "Cost grouped by the `metadata.prompt` version tag — did v4 cost less than v3?\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_prompt_costs",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (inclusive); defaults to 30 days before `until`",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (inclusive); defaults to 30 days before `until`",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (exclusive); defaults to now",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (exclusive); defaults to now",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/CostByDimension"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Cost grouped by the `metadata.prompt` version tag — did v4 cost less than v3?"
      }
    },
    "/v1/costs/unpriced": {
      "get": {
        "description": "Which models carried traffic the price book could not cost, loudest first.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_unpriced_models",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (default: 30 days ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (default: 30 days ago)",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ since, models: [{provider, model, calls, …}], unpriced_calls, notes, price_book: {verified_at, stale, …} } — the pairs carrying traffic the book cannot cost, ranked by calls, beside the freshness of the rates that did apply.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Which models carried traffic the price book could not cost, loudest first."
      }
    },
    "/v1/datasets/{id}": {
      "get": {
        "description": "One dataset by id; another project's is not found rather than refused.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_dataset",
        "parameters": [
          {
            "description": "dataset id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Dataset"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One dataset by id; another project's is not found rather than refused."
      }
    },
    "/v1/datasets/{id}/fork": {
      "post": {
        "description": "Fork a frozen dataset into the next version of its name, labels and all.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "fork_dataset",
        "parameters": [
          {
            "description": "id of the dataset to fork",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "id of the dataset to fork",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Dataset"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Fork a frozen dataset into the next version of its name, labels and all."
      }
    },
    "/v1/datasets/{id}/freeze": {
      "post": {
        "description": "Freeze a dataset so a finished run's input half stays fixed.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "freeze_dataset",
        "parameters": [
          {
            "description": "dataset id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Dataset"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Freeze a dataset so a finished run's input half stays fixed."
      }
    },
    "/v1/datasets/{id}/items": {
      "get": {
        "description": "The cases in one dataset.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_dataset_items",
        "parameters": [
          {
            "description": "dataset id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/DatasetItem"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The cases in one dataset."
      },
      "post": {
        "description": "Append one case; 409 if the dataset is frozen.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "add_dataset_item",
        "parameters": [
          {
            "description": "dataset id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "context": {
                    "type": "string"
                  },
                  "expected": {
                    "description": "golden reference answer",
                    "type": "string"
                  },
                  "input": {
                    "description": "the prompt / case input",
                    "type": "string"
                  },
                  "output": {
                    "description": "a captured/candidate response",
                    "type": "string"
                  },
                  "tags": {
                    "type": "array"
                  }
                },
                "required": [
                  "input"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetItem"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Append one case; 409 if the dataset is frozen."
      }
    },
    "/v1/datasets/{id}/items/from-label": {
      "post": {
        "description": "Promote a labelled production event into a golden case, copying the human verdict onto it.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "dataset_item_from_label",
        "parameters": [
          {
            "description": "dataset id to promote into",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id to promote into",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "label_id": {
                    "description": "the label to promote; its subject must be an event",
                    "type": "string"
                  }
                },
                "required": [
                  "label_id"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetItem"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Promote a labelled production event into a golden case, copying the human verdict onto it."
      }
    },
    "/v1/datasets/{id}/items/import": {
      "post": {
        "description": "Mine stored events or verdicts into an unfrozen dataset; 409 if it is frozen.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "import_dataset_items",
        "parameters": [
          {
            "description": "id of the dataset to import into",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "id of the dataset to import into",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "below": {
                    "description": "with from=scores: only verdicts whose normalised value (value/max) is below this",
                    "type": "number"
                  },
                  "dedupe": {
                    "description": "skip cases whose normalised input is already in the set (default false)",
                    "type": "boolean"
                  },
                  "event_ids": {
                    "description": "import exactly these events, bypassing the filter and strategy",
                    "type": "array"
                  },
                  "from": {
                    "description": "which table the cases come from (default events)",
                    "enum": [
                      "events",
                      "scores"
                    ],
                    "type": "string"
                  },
                  "model": {
                    "description": "only events from this model",
                    "type": "string"
                  },
                  "n": {
                    "description": "how many to mine (default 50, cap 5000)",
                    "type": "integer"
                  },
                  "since": {
                    "description": "RFC3339: only events at or after this instant",
                    "type": "string"
                  },
                  "status": {
                    "description": "only events with this outcome",
                    "enum": [
                      "success",
                      "error",
                      "timeout"
                    ],
                    "type": "string"
                  },
                  "strategy": {
                    "description": "how to choose them (default recent)",
                    "enum": [
                      "recent",
                      "random",
                      "stratified",
                      "errors"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ dataset_id, imported } — cases actually WRITTEN, not matched: with `dedupe`, 0 is a successful answer.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Mine stored events or verdicts into an unfrozen dataset; 409 if it is frozen."
      }
    },
    "/v1/datasets/{id}/labels": {
      "get": {
        "description": "Every human verdict on this set's items — the join `lt-runner calibrate --dataset` reads.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_dataset_labels",
        "parameters": [
          {
            "description": "dataset id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "dataset id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Label"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Every human verdict on this set's items — the join `lt-runner calibrate --dataset` reads."
      }
    },
    "/v1/events": {
      "get": {
        "description": "Recent events, newest first; keyset pagination through `X-Next-Cursor`.\n\nAn admin, or a project key carrying the `read` capability.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "list_events",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "max events (default 20, max 1000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max events (default 20, max 1000)",
              "type": "integer"
            }
          },
          {
            "description": "RFC3339 lower bound on event time (inclusive)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 lower bound on event time (inclusive)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 upper bound on event time (exclusive)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 upper bound on event time (exclusive)",
              "type": "string"
            }
          },
          {
            "description": "exact provider match (anthropic, openai, …)",
            "in": "query",
            "name": "provider",
            "required": false,
            "schema": {
              "description": "exact provider match (anthropic, openai, …)",
              "type": "string"
            }
          },
          {
            "description": "exact model match",
            "in": "query",
            "name": "model",
            "required": false,
            "schema": {
              "description": "exact model match",
              "type": "string"
            }
          },
          {
            "description": "only events in this trace",
            "in": "query",
            "name": "trace_id",
            "required": false,
            "schema": {
              "description": "only events in this trace",
              "type": "string"
            }
          },
          {
            "description": "use-case name filter (a call's `name`)",
            "in": "query",
            "name": "name",
            "required": false,
            "schema": {
              "description": "use-case name filter (a call's `name`)",
              "type": "string"
            }
          },
          {
            "description": "keep only events of this status",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "description": "keep only events of this status",
              "enum": [
                "success",
                "error"
              ],
              "type": "string"
            }
          },
          {
            "description": "only events carrying this tag",
            "in": "query",
            "name": "tag",
            "required": false,
            "schema": {
              "description": "only events carrying this tag",
              "type": "string"
            }
          },
          {
            "description": "a metadata predicate: `key` or `key=value`",
            "in": "query",
            "name": "meta",
            "required": false,
            "schema": {
              "description": "a metadata predicate: `key` or `key=value`",
              "type": "string"
            }
          },
          {
            "description": "minimum event cost (USD)",
            "in": "query",
            "name": "min_cost",
            "required": false,
            "schema": {
              "description": "minimum event cost (USD)",
              "type": "number"
            }
          },
          {
            "description": "1 also returns X-Total-Count over the whole match set",
            "in": "query",
            "name": "count",
            "required": false,
            "schema": {
              "description": "1 also returns X-Total-Count over the whole match set",
              "type": "integer"
            }
          },
          {
            "description": "keyset cursor from a prior call's next_cursor",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "keyset cursor from a prior call's next_cursor",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/LlmEvent"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Recent events, newest first; keyset pagination through `X-Next-Cursor`."
      },
      "post": {
        "description": "Ingest one LLM call event; cost is computed and the project's limits are evaluated.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "post_event",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LlmEvent"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ id, cost_usd, limit: {throttle, warning, binding_scope, binding_rule} } — the stored event's id plus the admission verdict the caps produced.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Ingest one LLM call event; cost is computed and the project's limits are evaluated."
      }
    },
    "/v1/events/batch": {
      "post": {
        "description": "Ingest an array of events; each item is accepted, rejected or invalid on its own.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "post_events_batch",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "items": {
                  "$ref": "#/components/schemas/LlmEvent"
                },
                "type": "array"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ accepted, rejected, invalid, results: [{index, status, id?, error?}] } — per-item outcomes under HTTP 200; one bad row never fails the batch.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Ingest an array of events; each item is accepted, rejected or invalid on its own."
      }
    },
    "/v1/events/{id}": {
      "get": {
        "description": "One event by id.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_event",
        "parameters": [
          {
            "description": "event id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "event id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlmEvent"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One event by id."
      }
    },
    "/v1/forecast": {
      "get": {
        "description": "Projected spend, budget-breach ETAs and margin-erosion crossovers for a project.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_forecast",
        "parameters": [
          {
            "description": "required for an admin key; a project key derives it",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "required for an admin key; a project key derives it",
              "type": "string"
            }
          },
          {
            "description": "margin dimension (default customer)",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "margin dimension (default customer)",
              "enum": [
                "customer",
                "product"
              ],
              "type": "string"
            }
          },
          {
            "description": "days to project ahead (default 14, 1..=90)",
            "in": "query",
            "name": "horizon",
            "required": false,
            "schema": {
              "description": "days to project ahead (default 14, 1..=90)",
              "type": "integer"
            }
          },
          {
            "description": "trailing days of history to fit (default 14, clamped to 4..=90 — below the evidence floor a trend cannot be presented)",
            "in": "query",
            "name": "lookback",
            "required": false,
            "schema": {
              "description": "trailing days of history to fit (default 14, clamped to 4..=90 — below the evidence floor a trend cannot be presented)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ project_id, generated_at, dimension, horizon_days, lookback_days, spend, budgets, margins, alerts, refused: [{subject, reason}] } — a projection under the evidence floor is withheld and named in `refused[]` rather than guessed.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Projected spend, budget-breach ETAs and margin-erosion crossovers for a project."
      }
    },
    "/v1/ingest/status": {
      "get": {
        "description": "Load shedding: in-flight depth plus the shed and timeout counters.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_ingest_status",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ in_flight, capacity, shed_total, timeout_total, … } — the load-shedding view of the ingest doors.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Load shedding: in-flight depth plus the shed and timeout counters."
      }
    },
    "/v1/jobs": {
      "get": {
        "description": "Recent queue rows, newest first; scoped so a key never reads another tenant's payloads.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_jobs",
        "parameters": [
          {
            "description": "keep only jobs in this state",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "description": "keep only jobs in this state",
              "enum": [
                "queued",
                "running",
                "cancelling",
                "cancelled",
                "done",
                "failed"
              ],
              "type": "string"
            }
          },
          {
            "description": "max jobs (default 50, max 1000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max jobs (default 50, max 1000)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Job"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Recent queue rows, newest first; scoped so a key never reads another tenant's payloads."
      },
      "post": {
        "description": "Enqueue one unit of background work of any known kind; an unknown type is a 400.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "enqueue_job",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "payload": {
                    "description": "kind-specific fields, validated against `type` at the door",
                    "type": "object"
                  },
                  "type": {
                    "description": "the job kind",
                    "enum": [
                      "bench_run",
                      "score_events",
                      "score_traces",
                      "dataset_sample",
                      "calibrate"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "type"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Job"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Enqueue one unit of background work of any known kind; an unknown type is a 400."
      }
    },
    "/v1/jobs/claim": {
      "post": {
        "description": "Atomically claim one queued (or stale-leased) job of a kind this worker can run.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "claim_job",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "kinds": {
                    "description": "the job kinds this worker can execute; empty means any",
                    "type": "array"
                  },
                  "providers": {
                    "description": "the providers this worker holds credentials for (advisory; logged, not filtered)",
                    "type": "array"
                  },
                  "stale_secs": {
                    "description": "how long a lease may go unrenewed before it is reclaimable (default 120)",
                    "type": "integer"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "A `Job` — the row this worker now holds, with its `claimed_at` lease fence — or `null` when nothing claimable is queued.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Atomically claim one queued (or stale-leased) job of a kind this worker can run."
      }
    },
    "/v1/jobs/{id}": {
      "get": {
        "description": "One job: status, progress, attempts, error and result.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_job",
        "parameters": [
          {
            "description": "job id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "job id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Job"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One job: status, progress, attempts, error and result."
      }
    },
    "/v1/jobs/{id}/cancel": {
      "post": {
        "description": "Ask a queued or running job to stop; cancelling a finished job is a 409.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "cancel_job",
        "parameters": [
          {
            "description": "job id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "job id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ outcome: \"cancelled\" | \"cancelling\" } — a queued job is stopped outright, a running one is marked `cancelling` and stops at its next case boundary. A job that already reached a terminal state is a 409, never a silent success.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Ask a queued or running job to stop; cancelling a finished job is a 409."
      }
    },
    "/v1/jobs/{id}/finish": {
      "post": {
        "description": "Record a job's verdict, conditioned on it being non-terminal and still held.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "job_finish",
        "parameters": [
          {
            "description": "job id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "job id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "claimed_at": {
                    "description": "the lease fence; omitted for an operator-shaped finish",
                    "type": "string"
                  },
                  "error": {
                    "description": "why it failed, when it did",
                    "type": "string"
                  },
                  "result": {
                    "description": "the verdict payload",
                    "type": "object"
                  },
                  "status": {
                    "description": "the terminal status to record (done | failed | cancelled)",
                    "type": "string"
                  }
                },
                "required": [
                  "status"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ ok: true } — or a **409** naming the lease that beat this one, meaning the verdict sent was NOT recorded.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record a job's verdict, conditioned on it being non-terminal and still held."
      }
    },
    "/v1/jobs/{id}/progress": {
      "post": {
        "description": "Record a running job's progress line; carried apart from the heartbeat on purpose.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "job_progress",
        "parameters": [
          {
            "description": "job id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "job id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "progress": {
                    "description": "a short human-readable progress line",
                    "type": "string"
                  }
                },
                "required": [
                  "progress"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ ok: true }",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record a running job's progress line; carried apart from the heartbeat on purpose."
      }
    },
    "/v1/jobs/{id}/renew": {
      "post": {
        "description": "Heartbeat: extend this worker's lease, or learn by 409 that it lost the job.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "job_renew",
        "parameters": [
          {
            "description": "job id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "job id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "claimed_at": {
                    "description": "the lease fence handed out at claim — proof the job is still this worker's",
                    "type": "string"
                  }
                },
                "required": [
                  "claimed_at"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ claimed_at } — the extended lease. A **409** means the lease is no longer this worker's (reaped, requeued or reclaimed) and the run must stop.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Heartbeat: extend this worker's lease, or learn by 409 that it lost the job."
      }
    },
    "/v1/judges/trust": {
      "get": {
        "description": "Whether one judge may be believed for one rubric, and the record that decided it.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_judge_trust",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "the judge model, e.g. anthropic/claude-haiku-4-5",
            "in": "query",
            "name": "judge",
            "required": true,
            "schema": {
              "description": "the judge model, e.g. anthropic/claude-haiku-4-5",
              "type": "string"
            }
          },
          {
            "description": "omit for the freeform (rubric-less) judge; a rubric never inherits that trust",
            "in": "query",
            "name": "rubric_id",
            "required": false,
            "schema": {
              "description": "omit for the freeform (rubric-less) judge; a rubric never inherits that trust",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ trust: trusted|untrusted|unknown, calibration: CalibrationRecord? } — `unknown` is 'never measured', never 'failed'.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Whether one judge may be believed for one rubric, and the record that decided it."
      }
    },
    "/v1/labels": {
      "get": {
        "description": "The human verdict ledger, newest first.\n\nAn admin, or a project key carrying the `read` capability.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "list_labels",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "'<kind>:<id>' with kind one of event, dataset_item, score",
            "in": "query",
            "name": "subject",
            "required": false,
            "schema": {
              "description": "'<kind>:<id>' with kind one of event, dataset_item, score",
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "rubric_id",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "type": "integer"
            }
          },
          {
            "description": "opaque keyset cursor from a previous page",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "opaque keyset cursor from a previous page",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ labels: [Label], next_cursor } — the cursor rides in the body here, not a header.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The human verdict ledger, newest first."
      },
      "post": {
        "description": "Record one human verdict; `labeler` is required so a calibration stays auditable.\n\nAn admin, or a project key carrying the `manage` capability.",
        "operationId": "post_label",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "dimensions": {
                    "description": "per-dimension human scores, when the grade was structured",
                    "type": "array"
                  },
                  "labeler": {
                    "description": "who said so — a person, a team",
                    "type": "string"
                  },
                  "note": {
                    "type": "string"
                  },
                  "pass": {
                    "description": "an explicit human pass/fail; omit to derive it from `value`",
                    "type": "boolean"
                  },
                  "project_id": {
                    "description": "project id (required with an admin key)",
                    "type": "string"
                  },
                  "rubric_id": {
                    "description": "the rubric this opinion was formed under, if any",
                    "type": "string"
                  },
                  "subject": {
                    "description": "'<kind>:<id>' with kind one of event, dataset_item, score",
                    "type": "string"
                  },
                  "value": {
                    "description": "overall quality 0-1, on the same scale a judge verdict normalizes to",
                    "type": "number"
                  }
                },
                "required": [
                  "subject",
                  "value",
                  "labeler"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Label"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record one human verdict; `labeler` is required so a calibration stays auditable."
      }
    },
    "/v1/limits/status": {
      "get": {
        "description": "Evaluate a project's limits now: per-rule status, the throttle flag, cost provenance.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "limits_status",
        "parameters": [
          {
            "description": "project id; required unless the key already names one",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "project id; required unless the key already names one",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ project_id, throttled, statuses: [LimitStatus], rejected: [{metric, window, scope, …}], cost_basis: {unpriced_calls, imputed_cost_usd, client_reported_cost_usd, unpriceable, derived_thresholds, inert_thresholds, notes} } — the caps evaluated now, plus how much of them rests on weak cost evidence.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Evaluate a project's limits now: per-rule status, the throttle flag, cost provenance."
      }
    },
    "/v1/limits/usage": {
      "get": {
        "description": "Rolling usage broken down by one scope dimension — the 'who is spending' view.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "limits_usage",
        "parameters": [
          {
            "description": "project id; required unless the key already names one",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "project id; required unless the key already names one",
              "type": "string"
            }
          },
          {
            "description": "the dimension to group by (default api_key)",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "the dimension to group by (default api_key)",
              "enum": [
                "api_key",
                "customer",
                "model",
                "provider",
                "name"
              ],
              "type": "string"
            }
          },
          {
            "description": "rolling window (default day)",
            "in": "query",
            "name": "window",
            "required": false,
            "schema": {
              "description": "rolling window (default day)",
              "enum": [
                "hour",
                "day",
                "month"
              ],
              "type": "string"
            }
          },
          {
            "description": "max rows (default 20, max 200)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max rows (default 20, max 200)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ project_id, by, window, since, total, entries: [{value, label?, …usage, cost_share_pct, rules: [LimitStatus]}], truncated? } — who is spending, and every scoped rule that currently binds them.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Rolling usage broken down by one scope dimension — the 'who is spending' view."
      }
    },
    "/v1/limits/{id}": {
      "delete": {
        "description": "Remove a limit rule by id.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "delete_limit",
        "parameters": [
          {
            "description": "limit rule id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "limit rule id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ deleted: id } — the rule that was removed.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Remove a limit rule by id."
      },
      "put": {
        "description": "Replace a limit rule wholesale; the sweep-owned fields are carried over, not cleared.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "update_limit",
        "parameters": [
          {
            "description": "limit rule id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "limit rule id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "action": {
                    "description": "what a breach does (default alert)",
                    "enum": [
                      "alert",
                      "throttle",
                      "block"
                    ],
                    "type": "string"
                  },
                  "enabled": {
                    "description": "toggle the rule on/off (default true)",
                    "type": "boolean"
                  },
                  "metric": {
                    "description": "what is capped",
                    "enum": [
                      "cost_usd",
                      "calls",
                      "tokens"
                    ],
                    "type": "string"
                  },
                  "scope": {
                    "description": "dimension scope, e.g. {\"model\":\"gpt-4o\"}",
                    "type": "object"
                  },
                  "threshold": {
                    "description": "a fixed cap, or `{\"pct\": N}` for one derived from recognized revenue",
                    "type": "number"
                  },
                  "warn_at": {
                    "description": "soft-warning fraction of threshold in (0,1)",
                    "type": "number"
                  },
                  "window": {
                    "description": "the rolling window",
                    "enum": [
                      "hour",
                      "day",
                      "month"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "metric",
                  "window",
                  "threshold"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LimitRule"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Replace a limit rule wholesale; the sweep-owned fields are carried over, not cleared."
      }
    },
    "/v1/margin": {
      "get": {
        "description": "Revenue minus LLM cost by customer or product over a window.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_margin",
        "parameters": [
          {
            "description": "group dimension (default customer)",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "group dimension (default customer)",
              "enum": [
                "customer",
                "product"
              ],
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (default 30d ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (default 30d ago)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (default now)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (default now)",
              "type": "string"
            }
          },
          {
            "description": "at-risk cohort: keep only rows whose margin% is under this (e.g. 0 = loss-making)",
            "in": "query",
            "name": "below",
            "required": false,
            "schema": {
              "description": "at-risk cohort: keep only rows whose margin% is under this (e.g. 0 = loss-making)",
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ dimension, since, until, total_revenue_usd, total_cost_usd, total_margin_usd, unconverted_currencies?, currency_note?, below?, rows: [{key, revenue_usd, llm_cost_usd, gross_margin_usd, margin_pct, calls, guardrail?}] } — when `below` is echoed, the totals are the filtered cohort's, not the window's.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Revenue minus LLM cost by customer or product over a window."
      }
    },
    "/v1/margin/customer/{id}": {
      "get": {
        "description": "One customer's windowed revenue and cost, split by model and by use-case.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_customer_margin",
        "parameters": [
          {
            "description": "customer id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "customer id",
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (default 30d ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (default 30d ago)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (default now)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (default now)",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ customer_id, since, until, revenue_usd, cost_usd, margin_usd, margin_pct, by_model: [{key, cost_usd, calls}], by_name: […] } — one customer's window, with the cost split by `provider/model` and by use-case name, dearest first.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One customer's windowed revenue and cost, split by model and by use-case."
      }
    },
    "/v1/margin/simulate": {
      "get": {
        "description": "Pricing what-if: margin recomputed under a hypothetical price model; nothing is stored.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_margin_simulate",
        "parameters": [
          {
            "description": "group dimension (default customer)",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "group dimension (default customer)",
              "enum": [
                "customer",
                "product"
              ],
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "hypothetical charge per 1M prompt+completion tokens",
            "in": "query",
            "name": "price_per_mtok",
            "required": false,
            "schema": {
              "description": "hypothetical charge per 1M prompt+completion tokens",
              "type": "number"
            }
          },
          {
            "description": "hypothetical flat charge per key per 30-day month",
            "in": "query",
            "name": "flat_monthly",
            "required": false,
            "schema": {
              "description": "hypothetical flat charge per key per 30-day month",
              "type": "number"
            }
          },
          {
            "description": "RFC3339 window start (default 30d ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (default 30d ago)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (default now)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (default now)",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ simulated: true, dimension, since, until, assumptions, total_actual_margin_usd, total_simulated_margin_usd, total_margin_delta_usd, unconverted_currencies?, currency_note?, rows: [{key, actual_margin_usd, simulated_margin_usd, …}] } — read-only; at least one of the two price parameters is required (else 400).",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Pricing what-if: margin recomputed under a hypothetical price model; nothing is stored."
      }
    },
    "/v1/margin/trend": {
      "get": {
        "description": "Per-day revenue/cost/margin series for the top keys of a dimension.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_margin_trend",
        "parameters": [
          {
            "description": "group dimension (default customer)",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "group dimension (default customer)",
              "enum": [
                "customer",
                "product"
              ],
              "type": "string"
            }
          },
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "trailing window length (default 30, clamped to 1..=365)",
            "in": "query",
            "name": "days",
            "required": false,
            "schema": {
              "description": "trailing window length (default 30, clamped to 1..=365)",
              "type": "integer"
            }
          },
          {
            "description": "max keys by |total margin| (default 20)",
            "in": "query",
            "name": "top",
            "required": false,
            "schema": {
              "description": "max keys by |total margin| (default 20)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ dimension, since, until, days, key_count, top_n, totals: {…dense daily series…}, series: [{key, …per-day revenue/cost/margin…}] } — `key_count` is the pre-cap key count, so a client can say \"showing top_n of key_count\".",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Per-day revenue/cost/margin series for the top keys of a dimension."
      }
    },
    "/v1/prices": {
      "get": {
        "description": "The rate in force today for every model in the DB-backed price book.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_prices",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ModelPriceRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The rate in force today for every model in the DB-backed price book."
      }
    },
    "/v1/prices/history/{provider}/{model}": {
      "get": {
        "description": "One model's price timeline, newest first — what a call in a past window really cost.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_price_history",
        "parameters": [
          {
            "description": "provider id",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "description": "provider id",
              "type": "string"
            }
          },
          {
            "description": "model id",
            "in": "path",
            "name": "model",
            "required": true,
            "schema": {
              "description": "model id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ModelPriceRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One model's price timeline, newest first — what a call in a past window really cost."
      }
    },
    "/v1/prices/{provider}/{model}": {
      "put": {
        "description": "Upsert one model's rate, hot-swapping the live book and optionally pricing its history.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "put_price",
        "parameters": [
          {
            "description": "provider id",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "description": "provider id",
              "type": "string"
            }
          },
          {
            "description": "model id",
            "in": "path",
            "name": "model",
            "required": true,
            "schema": {
              "description": "model id",
              "type": "string"
            }
          },
          {
            "description": "`1`/`true` to price this key's stored `cost_usd IS NULL` rows from the new rate",
            "in": "query",
            "name": "fill_unpriced",
            "required": false,
            "schema": {
              "description": "`1`/`true` to price this key's stored `cost_usd IS NULL` rows from the new rate",
              "type": "string"
            }
          },
          {
            "description": "how far back the post-fill recount looks (default 90 days)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "how far back the post-fill recount looks (default 90 days)",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "cached_input_per_mtok": {
                    "description": "cached-input rate, USD per million tokens",
                    "type": "number"
                  },
                  "effective_from": {
                    "description": "when the rate takes effect (date or RFC3339); defaults to now",
                    "type": "string"
                  },
                  "input_per_mtok": {
                    "description": "input rate, USD per million tokens",
                    "type": "number"
                  },
                  "note": {
                    "description": "free-text: why the rate changed, a ticket id, a caveat",
                    "type": "string"
                  },
                  "output_per_mtok": {
                    "description": "output rate, USD per million tokens",
                    "type": "number"
                  },
                  "source_url": {
                    "description": "where the rate was read from",
                    "type": "string"
                  },
                  "verified_at": {
                    "description": "when a human last checked it against the vendor",
                    "type": "string"
                  }
                },
                "required": [
                  "input_per_mtok",
                  "output_per_mtok"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The stored ModelPriceRow, flattened, plus `filled` and `remaining_unpriced` when a fill ran — `null` (absent) when none was asked for, which is not the same as `0`.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Upsert one model's rate, hot-swapping the live book and optionally pricing its history."
      }
    },
    "/v1/projects": {
      "get": {
        "description": "Every project on this deployment.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_projects",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Project"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Every project on this deployment."
      },
      "post": {
        "description": "Create a project; a caller-supplied id is validated and honoured, else one is minted.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_project",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "name": {
                    "description": "human-readable project name",
                    "type": "string"
                  },
                  "redaction": {
                    "description": "payload persistence policy (default none)",
                    "enum": [
                      "none",
                      "hash",
                      "drop"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "name"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Project"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Create a project; a caller-supplied id is validated and honoured, else one is minted."
      }
    },
    "/v1/projects/{id}": {
      "delete": {
        "description": "Archive a project: disabled and stamped `archived_at`, with every row kept.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "archive_project",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Project"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Archive a project: disabled and stamped `archived_at`, with every row kept."
      },
      "put": {
        "description": "Update a project; an omitted field is left as-is and the policy cache is invalidated.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "update_project",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "collective_opt_in": {
                    "description": "consent to collective digests",
                    "type": "boolean"
                  },
                  "enabled": {
                    "description": "false stops this project's keys opening anything",
                    "type": "boolean"
                  },
                  "name": {
                    "description": "new name",
                    "type": "string"
                  },
                  "redaction": {
                    "description": "payload persistence policy; enforced on the NEXT ingested event",
                    "enum": [
                      "none",
                      "hash",
                      "drop"
                    ],
                    "type": "string"
                  },
                  "require_trusted_judge": {
                    "description": "refuse promotion on an untrusted judge",
                    "type": "boolean"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Project"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Update a project; an omitted field is left as-is and the policy cache is invalidated."
      }
    },
    "/v1/projects/{id}/alert-channels": {
      "get": {
        "description": "The channels this project's alerts effectively reach: its own plus the inherited globals.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_alert_channels",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AlertChannel"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The channels this project's alerts effectively reach: its own plus the inherited globals."
      },
      "put": {
        "description": "Add a routing destination for a project's alerts; a signed one is minted a secret once.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "put_alert_channel",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "enabled": {
                    "description": "deliver to it (default true)",
                    "type": "boolean"
                  },
                  "kind": {
                    "description": "the destination's transport",
                    "enum": [
                      "webhook",
                      "ntfy",
                      "email"
                    ],
                    "type": "string"
                  },
                  "kinds": {
                    "description": "alert kinds this channel wants; empty = every kind",
                    "type": "array"
                  },
                  "min_severity": {
                    "description": "severity floor for this channel (default info)",
                    "enum": [
                      "info",
                      "warning",
                      "critical"
                    ],
                    "type": "string"
                  },
                  "signed": {
                    "description": "sign this channel's deliveries; the secret is returned once and never stored",
                    "type": "boolean"
                  },
                  "target": {
                    "description": "the URL (webhook/ntfy) or address (email)",
                    "type": "string"
                  }
                },
                "required": [
                  "kind",
                  "target"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The redacted AlertChannel, plus `secret` and `secret_note` when `signed` — the plaintext signing secret, shown exactly once.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Add a routing destination for a project's alerts; a signed one is minted a secret once."
      }
    },
    "/v1/projects/{id}/alert-channels/{cid}": {
      "delete": {
        "description": "Remove one stored routing destination.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "delete_alert_channel",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "alert channel id",
            "in": "path",
            "name": "cid",
            "required": true,
            "schema": {
              "description": "alert channel id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ deleted } — the channel id that was removed.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Remove one stored routing destination."
      }
    },
    "/v1/projects/{id}/benchmarks": {
      "get": {
        "description": "A project's benchmark definitions.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_benchmarks",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Benchmark"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's benchmark definitions."
      },
      "post": {
        "description": "Define a benchmark: a dataset, a rubric, a judge, and optionally a target matrix.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_benchmark",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "baseline_score": {
                    "description": "the mean a run must not fall below",
                    "type": "number"
                  },
                  "dataset": {
                    "description": "inline benchmark cases",
                    "items": {
                      "properties": {
                        "expected": {
                          "description": "golden reference answer the judge can compare against (optional)",
                          "type": "string"
                        },
                        "input": {
                          "description": "the case prompt / input",
                          "type": "string"
                        },
                        "output": {
                          "description": "a pre-captured candidate response to judge; omit to generate from targets (optional)",
                          "type": "string"
                        }
                      },
                      "required": [
                        "input"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "dataset_ref": {
                    "description": "stored dataset id, instead of (or beside) `dataset`",
                    "type": "string"
                  },
                  "judge_model": {
                    "description": "[provider/]model (default opus@xhigh)",
                    "type": "string"
                  },
                  "name": {
                    "description": "benchmark name",
                    "type": "string"
                  },
                  "rubric": {
                    "description": "freeform rubric text (single-score mode)",
                    "type": "string"
                  },
                  "rubric_id": {
                    "description": "structured rubric id (per-dimension mode)",
                    "type": "string"
                  },
                  "schedule_interval_secs": {
                    "description": "opt-in recurrence; rejected for a comparison-matrix target",
                    "type": "integer"
                  },
                  "target": {
                    "deprecated": true,
                    "description": "single generation target; superseded by `targets`\n\nDEPRECATED (advertised), removed in 0.2.0: send a one-element `targets` array instead. Policy: docs/DEPRECATION.md.",
                    "type": "object",
                    "x-deprecation-stage": "advertised",
                    "x-removed-in": "0.2.0"
                  },
                  "targets": {
                    "description": "comparison matrix: one row per target under test. A target is a (provider, model) with either a literal system_prompt or a prompt_ref that resolves from the registry at run time, or an HTTP endpoint of your own (kind.type=http) that receives {input, expected?, system_prompt?} and returns {output, usage?, latency_ms?, cost_usd?}.",
                    "items": {
                      "properties": {
                        "kind": {
                          "description": "how this target produces output. Omit for a model call.",
                          "properties": {
                            "type": {
                              "enum": [
                                "model",
                                "http"
                              ],
                              "type": "string"
                            },
                            "url": {
                              "description": "https endpoint (http target only). Private, loopback and link-local addresses are refused.",
                              "type": "string"
                            }
                          },
                          "required": [
                            "type"
                          ],
                          "type": "object"
                        },
                        "label": {
                          "description": "display label; defaults to provider/model",
                          "type": "string"
                        },
                        "model": {
                          "type": "string"
                        },
                        "prompt_ref": {
                          "description": "resolve this target's prompt from the registry at run start instead of using the literal system_prompt. Required for the promotion gate to certify the version it ran: a run only reports resolved_prompt_version when it fetched the content. Pass at most one of version/label.",
                          "properties": {
                            "label": {
                              "description": "resolve through a label, e.g. production",
                              "type": "string"
                            },
                            "name": {
                              "description": "registry name, must already exist in this project",
                              "type": "string"
                            },
                            "version": {
                              "description": "pin an exact version",
                              "type": "integer"
                            }
                          },
                          "required": [
                            "name"
                          ],
                          "type": "object"
                        },
                        "provider": {
                          "description": "e.g. anthropic, openai; for an http target, a label for whatever answers there",
                          "type": "string"
                        },
                        "system_prompt": {
                          "description": "literal system/instruction prompt variant under test",
                          "type": "string"
                        }
                      },
                      "required": [
                        "provider",
                        "model"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  }
                },
                "required": [
                  "name"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Benchmark"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Define a benchmark: a dataset, a rubric, a judge, and optionally a target matrix."
      }
    },
    "/v1/projects/{id}/datasets": {
      "get": {
        "description": "A project's datasets.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_datasets",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Dataset"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's datasets."
      },
      "post": {
        "description": "Create a dataset — version 1, unfrozen, the root of its own lineage.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_dataset",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "name": {
                    "description": "dataset name; its versions share it",
                    "type": "string"
                  },
                  "source": {
                    "description": "provenance label, e.g. manual or events:recent",
                    "type": "string"
                  }
                },
                "required": [
                  "name"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Dataset"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Create a dataset — version 1, unfrozen, the root of its own lineage."
      }
    },
    "/v1/projects/{id}/datasets/versions": {
      "get": {
        "description": "Every version of one dataset name, newest first — what a run's dataset_pin resolves to.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_dataset_versions",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "the dataset name to walk — a query parameter because a name routinely contains `/`",
            "in": "query",
            "name": "name",
            "required": true,
            "schema": {
              "description": "the dataset name to walk — a query parameter because a name routinely contains `/`",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Dataset"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Every version of one dataset name, newest first — what a run's dataset_pin resolves to."
      }
    },
    "/v1/projects/{id}/keys": {
      "get": {
        "description": "A project's keys with their scopes, expiry, last use and revocation state.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_keys",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[{ id, name, prefix, created_at, last_used_at?, revoked, scopes, expires_at? }] — a key's non-secret metadata; never the hash and never the secret.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's keys with their scopes, expiry, last use and revocation state."
      },
      "post": {
        "description": "Mint an API key on a project; the secret is returned once and only once.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_key",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "expires_at": {
                    "description": "RFC3339 hard expiry",
                    "type": "string"
                  },
                  "name": {
                    "description": "key label (default `default`)",
                    "type": "string"
                  },
                  "scopes": {
                    "description": "ingest | read | manage; omitted ⇒ the back-compat default",
                    "type": "array"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ id, project_id, name, prefix, key, scopes, expires_at?, created_at } — `key` is the full secret, shown exactly once and never retrievable again.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Mint an API key on a project; the secret is returned once and only once."
      }
    },
    "/v1/projects/{id}/keys/{kid}": {
      "delete": {
        "description": "Revoke a key immediately; the row is kept for audit.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "revoke_key",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "key id",
            "in": "path",
            "name": "kid",
            "required": true,
            "schema": {
              "description": "key id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The revoked key's non-secret metadata, with `revoked: true`.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Revoke a key immediately; the row is kept for audit."
      }
    },
    "/v1/projects/{id}/keys/{kid}/rotate": {
      "post": {
        "description": "Rotate a key: mint a successor and give the predecessor a deadline, not a cliff.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "rotate_key",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "key id to rotate",
            "in": "path",
            "name": "kid",
            "required": true,
            "schema": {
              "description": "key id to rotate",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "grace_secs": {
                    "description": "how long the predecessor keeps working (0 retires it at once)",
                    "type": "integer"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ successor: {…, key}, predecessor: {…, expires_at} } — the new secret shown once, and the old key now carrying the deadline that closes the grace window.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Rotate a key: mint a successor and give the predecessor a deadline, not a cliff."
      }
    },
    "/v1/projects/{id}/limits": {
      "get": {
        "description": "A project's limit rules, enabled and disabled alike.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_limits",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/LimitRule"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's limit rules, enabled and disabled alike."
      },
      "post": {
        "description": "Add a usage-limit rule to a project; monitored ingest only, never the judge.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_limit",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "action": {
                    "description": "what a breach does (default alert)",
                    "enum": [
                      "alert",
                      "throttle",
                      "block"
                    ],
                    "type": "string"
                  },
                  "metric": {
                    "description": "what is capped",
                    "enum": [
                      "cost_usd",
                      "calls",
                      "tokens"
                    ],
                    "type": "string"
                  },
                  "threshold": {
                    "description": "a fixed cap, or `{\"pct\": N}` for one derived from recognized revenue",
                    "type": "number"
                  },
                  "window": {
                    "description": "the rolling window",
                    "enum": [
                      "hour",
                      "day",
                      "month"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "metric",
                  "window",
                  "threshold"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LimitRule"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Add a usage-limit rule to a project; monitored ingest only, never the judge."
      }
    },
    "/v1/projects/{id}/margin-policies": {
      "get": {
        "description": "A project's standing margin guardrails.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_margin_policies",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/MarginPolicy"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's standing margin guardrails."
      },
      "post": {
        "description": "Store a standing margin guardrail; the forecast sweep is what turns it into rules.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_margin_policy",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "action": {
                    "description": "the limit rule it creates when armed",
                    "type": "object"
                  },
                  "cooldown_secs": {
                    "description": "gap between actions (default 3600)",
                    "type": "integer"
                  },
                  "enabled": {
                    "description": "default true",
                    "type": "boolean"
                  },
                  "expiry_secs": {
                    "description": "how long a created rule lives (default 86400)",
                    "type": "integer"
                  },
                  "min_cost_usd": {
                    "description": "windowed cost a subject must exceed to be acted on",
                    "type": "number"
                  },
                  "trigger": {
                    "description": "the margin condition that arms the policy",
                    "type": "object"
                  }
                },
                "required": [
                  "trigger",
                  "action"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarginPolicy"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Store a standing margin guardrail; the forecast sweep is what turns it into rules."
      }
    },
    "/v1/projects/{id}/margin-policies/{pid}": {
      "delete": {
        "description": "Remove a margin policy; the rules it created are reaped by the sweep, not here.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "delete_margin_policy",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "margin policy id",
            "in": "path",
            "name": "pid",
            "required": true,
            "schema": {
              "description": "margin policy id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ deleted: id } — the policy that was removed.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Remove a margin policy; the rules it created are reaped by the sweep, not here."
      }
    },
    "/v1/projects/{id}/prompts": {
      "get": {
        "description": "A project's registry prompts with their label pointers and gating benchmark.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_prompts",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Prompt"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's registry prompts with their label pointers and gating benchmark."
      },
      "post": {
        "description": "Register a new prompt with its first version; 409 when the name already exists.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_prompt",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "benchmark_id": {
                    "description": "the benchmark whose regression check gates promotions",
                    "type": "string"
                  },
                  "config": {
                    "description": "structured config (model, params, variable schema)",
                    "type": "object"
                  },
                  "content": {
                    "description": "content of version 1",
                    "type": "string"
                  },
                  "name": {
                    "description": "registry prompt name (unique per project)",
                    "type": "string"
                  },
                  "note": {
                    "description": "change note for version 1",
                    "type": "string"
                  }
                },
                "required": [
                  "name",
                  "content"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ prompt: Prompt, version: PromptVersion, enqueued_job? } — the registry entry, its version 1, and the benchmark job the cut auto-enqueued.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Register a new prompt with its first version; 409 when the name already exists."
      }
    },
    "/v1/projects/{id}/prompts/{name}": {
      "get": {
        "description": "Runtime fetch: resolve a prompt by version, by label, or to its latest version.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_prompt",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          },
          {
            "description": "resolve the version this label points at (e.g. production)",
            "in": "query",
            "name": "label",
            "required": false,
            "schema": {
              "description": "resolve the version this label points at (e.g. production)",
              "type": "string"
            }
          },
          {
            "description": "resolve this exact version number",
            "in": "query",
            "name": "version",
            "required": false,
            "schema": {
              "description": "resolve this exact version number",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ id, name, version, label?, tag, content, config?, note? } — one resolved version; `tag` is the `\"<name>@v<n>\"` attribution string to stamp on the traffic it produces.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Runtime fetch: resolve a prompt by version, by label, or to its latest version."
      },
      "put": {
        "description": "Point an existing prompt at the benchmark whose regression check gates its promotions.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "link_prompt_benchmark",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "benchmark_id": {
                    "description": "the gating benchmark; null unlinks",
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Prompt"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Point an existing prompt at the benchmark whose regression check gates its promotions."
      }
    },
    "/v1/projects/{id}/prompts/{name}/canary": {
      "put": {
        "description": "Set or clear the online canary policy; a policy that could never fire is refused.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "set_prompt_canary",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "canary": {
                    "description": "the CanaryPolicy to store; null clears it",
                    "type": "object"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Prompt"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Set or clear the online canary policy; a policy that could never fire is refused."
      }
    },
    "/v1/projects/{id}/prompts/{name}/promote": {
      "post": {
        "description": "Point a label at a version, blocked (409) when the gating benchmark has regressed.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "promote_prompt",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "force": {
                    "description": "override the benchmark regression gate (default false)",
                    "type": "boolean"
                  },
                  "label": {
                    "description": "the label to move, e.g. production",
                    "type": "string"
                  },
                  "version": {
                    "description": "the version number the label should point at",
                    "type": "integer"
                  }
                },
                "required": [
                  "label",
                  "version"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "a Prompt, flattened, plus `warning?` when the gate could not verify the run generated with this version, and `judge_trust?` for the judge behind that evidence.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Point a label at a version, blocked (409) when the gating benchmark has regressed."
      }
    },
    "/v1/projects/{id}/prompts/{name}/versions": {
      "get": {
        "description": "Every stored version of one registry prompt.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_prompt_versions",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/PromptVersion"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Every stored version of one registry prompt."
      },
      "post": {
        "description": "Cut the next monotonic version; the linked benchmark is auto-enqueued against it.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "add_prompt_version",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "registry prompt name",
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "description": "registry prompt name",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "benchmark_id": {
                    "description": "gating benchmark — honored only when the prompt is created",
                    "type": "string"
                  },
                  "config": {
                    "description": "structured config (model, params, variable schema)",
                    "type": "object"
                  },
                  "content": {
                    "description": "the prompt text / template for this version",
                    "type": "string"
                  },
                  "note": {
                    "description": "change note describing why this version was cut",
                    "type": "string"
                  }
                },
                "required": [
                  "content"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ version: PromptVersion, enqueued_job? } — the new version and the benchmark job the cut auto-enqueued.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Cut the next monotonic version; the linked benchmark is auto-enqueued against it."
      }
    },
    "/v1/projects/{id}/redaction": {
      "get": {
        "description": "What the ingest boundary actually did to this project's stored rows.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_redaction_posture",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 lower bound on arrival time (default 30 days back)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 lower bound on arrival time (default 30 days back)",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ project_id, since, current_rules, unaccounted_events, total_events, groups: [{stamp, events, …}] } — what the stored rows actually had done to them, counted from the rows rather than from the configuration.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "What the ingest boundary actually did to this project's stored rows."
      }
    },
    "/v1/projects/{id}/rubrics": {
      "get": {
        "description": "A project's structured rubrics.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_rubrics",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Rubric"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A project's structured rubrics."
      },
      "post": {
        "description": "Create a structured rubric at generation 1.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_rubric",
        "parameters": [
          {
            "description": "project id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "project id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "dimensions": {
                    "description": "weighted scoring dimensions the judge fills per case",
                    "items": {
                      "properties": {
                        "anchors": {
                          "description": "anchored level descriptions, e.g. [\"1.0 = fully correct\", \"0.5 = minor error\", \"0 = wrong\"]",
                          "items": {
                            "type": "string"
                          },
                          "type": "array"
                        },
                        "description": {
                          "description": "what this dimension measures",
                          "type": "string"
                        },
                        "floor": {
                          "description": "gating floor: if this dimension scores below it, the case fails regardless of the overall",
                          "type": "number"
                        },
                        "key": {
                          "description": "stable identifier used in the judge's JSON output (e.g. correctness)",
                          "type": "string"
                        },
                        "weight": {
                          "description": "relative weight in the overall score (default 1.0)",
                          "type": "number"
                        }
                      },
                      "required": [
                        "key",
                        "description"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "name": {
                    "type": "string"
                  },
                  "threshold": {
                    "description": "overall pass threshold 0-1 (default 0.7)",
                    "type": "number"
                  }
                },
                "required": [
                  "name",
                  "dimensions"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Rubric"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Create a structured rubric at generation 1."
      }
    },
    "/v1/projects/{id}/schedules": {
      "get": {
        "description": "One project's schedules; a project key reads its own, a mismatch is a 403.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_project_schedules",
        "parameters": [
          {
            "description": "the project whose schedules to list",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "the project whose schedules to list",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Schedule"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One project's schedules; a project key reads its own, a mismatch is a 403."
      },
      "post": {
        "description": "Store a recurring workload; its payload is validated against its kind at creation.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_schedule",
        "parameters": [
          {
            "description": "the project this workload recurs for",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "the project this workload recurs for",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "enabled": {
                    "description": "default true; false stores it paused",
                    "type": "boolean"
                  },
                  "interval_secs": {
                    "description": "how often it fires (floor 60)",
                    "type": "integer"
                  },
                  "payload": {
                    "description": "the job payload enqueued each time it fires",
                    "type": "object"
                  },
                  "start_in_secs": {
                    "description": "seconds until the first firing (default 0 = at once)",
                    "type": "integer"
                  },
                  "type": {
                    "description": "the job kind enqueued each time it fires",
                    "enum": [
                      "bench_run",
                      "score_events",
                      "score_traces",
                      "dataset_sample",
                      "calibrate"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "type",
                  "interval_secs"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Schedule"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Store a recurring workload; its payload is validated against its kind at creation."
      }
    },
    "/v1/quality/prompts": {
      "get": {
        "description": "How each served prompt version is scoring in production, with n and a ~95% interval.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_prompt_quality",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 lower bound on the VERDICT time (default 7 days ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 lower bound on the VERDICT time (default 7 days ago)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 upper bound on the verdict time",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 upper bound on the verdict time",
              "type": "string"
            }
          },
          {
            "description": "narrow to one rubric — the only way two versions are compared on the same criteria",
            "in": "query",
            "name": "rubric_id",
            "required": false,
            "schema": {
              "description": "narrow to one rubric — the only way two versions are compared on the same criteria",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[{ tag, name?, version?, n, mean, pass_rate, ci95_low, ci95_high, cost_usd }] — one row per served version, newest first, with the untagged bucket last.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "How each served prompt version is scoring in production, with n and a ~95% interval."
      }
    },
    "/v1/relay/actions": {
      "get": {
        "description": "The action fingerprint ledger: which prompt text each action has been running, and when.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_relay_actions",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "settle events to walk (default 1000, cap 20000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "settle events to walk (default 1000, cap 20000)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ actions: [{action_type, prompt_sha256, versions, runs, errors, judgeable, first_seen, last_seen}], scanned, truncated } — one row per action × prompt fingerprint, derived from the settle events; `truncated` says the walk hit its ceiling.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The action fingerprint ledger: which prompt text each action has been running, and when."
      }
    },
    "/v1/relay/actions/{action_type}/dataset": {
      "post": {
        "description": "Snapshot an action's succeeded runs into a dataset so a benchmark can gate its prompt.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "snapshot_relay_action_dataset",
        "parameters": [
          {
            "description": "the namespaced action type; its `/` is percent-encoded in the path",
            "in": "path",
            "name": "action_type",
            "required": true,
            "schema": {
              "description": "the namespaced action type; its `/` is percent-encoded in the path",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "limit": {
                    "description": "succeeded tasks to snapshot (default 200, cap 1000)",
                    "type": "integer"
                  },
                  "name": {
                    "description": "dataset name (default `relay:<action_type>`)",
                    "type": "string"
                  },
                  "project_id": {
                    "description": "the project whose succeeded runs are snapshotted",
                    "type": "string"
                  }
                },
                "required": [
                  "project_id"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The created Dataset, flattened, plus `items` and `skipped` — runs that carried no usable (payload, result) pair are counted, not silently dropped.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Snapshot an action's succeeded runs into a dataset so a benchmark can gate its prompt."
      }
    },
    "/v1/relay/devices": {
      "get": {
        "description": "The enrolled fleet: capabilities, liveness, agent version, revocation.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_relay_devices",
        "parameters": [
          {
            "description": "one project's devices; operator-wide devices are always included",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "one project's devices; operator-wide devices are always included",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[Device (no key, no digest) + { seen_secs_ago, online }] — the fleet with the only liveness a device with no inbound path can have: when it last called in.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The enrolled fleet: capabilities, liveness, agent version, revocation."
      },
      "post": {
        "description": "Enrol a device and mint its key, which is shown exactly once.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_relay_device",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "capabilities": {
                    "description": "what it may run, exact or `ns/*`; omitted means everything",
                    "type": "array"
                  },
                  "name": {
                    "description": "human name for the machine, e.g. `studio-laptop`",
                    "type": "string"
                  },
                  "project_id": {
                    "description": "scope it to one project; omit for an operator-wide device",
                    "type": "string"
                  }
                },
                "required": [
                  "name"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The Device row, flattened, plus `key` — the raw `ltd_…` device key, shown here and nowhere else ever again; only its salted digest is stored.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Enrol a device and mint its key, which is shown exactly once."
      }
    },
    "/v1/relay/devices/{id}": {
      "delete": {
        "description": "Revoke a device: a flag, not a delete, so tasks it ran keep naming a device that resolves.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "revoke_relay_device",
        "parameters": [
          {
            "description": "device id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "device id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Device"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Revoke a device: a flag, not a delete, so tasks it ran keep naming a device that resolves."
      }
    },
    "/v1/relay/lease": {
      "post": {
        "description": "A device leases due tasks matching its capabilities, and is told how often to renew.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "lease_relay_tasks",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "agent_version": {
                    "description": "the `lt-agent` version, recorded on the device",
                    "type": "string"
                  },
                  "capabilities": {
                    "description": "action types this device can run, exact or `ns/*`; empty means no filter",
                    "type": "array"
                  },
                  "lease_secs": {
                    "description": "requested TTL, clamped to 60..1800",
                    "type": "integer"
                  },
                  "max": {
                    "description": "tasks to lease (default 1, cap 20)",
                    "type": "integer"
                  },
                  "wait_secs": {
                    "description": "long-poll: hold up to this many seconds for work (cap 25)",
                    "type": "integer"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ tasks: [RelayTask], renew_secs, lease_secs } — the leased work plus the renewal cadence and the TTL actually granted after clamping.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A device leases due tasks matching its capabilities, and is told how often to renew."
      }
    },
    "/v1/relay/tasks": {
      "get": {
        "description": "Relay tasks, newest first, filtered by project and status.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_relay_tasks",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "only tasks in this state; an unknown value is a 400, not an empty page",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "description": "only tasks in this state; an unknown value is a 400, not an empty page",
              "enum": [
                "queued",
                "leased",
                "succeeded",
                "dead",
                "cancelling",
                "cancelled"
              ],
              "type": "string"
            }
          },
          {
            "description": "max tasks (default 50, max 1000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max tasks (default 50, max 1000)",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/RelayTask"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Relay tasks, newest first, filtered by project and status."
      },
      "post": {
        "description": "Enqueue a device task; routability and the project's usage limits are checked first.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "enqueue_relay_task",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "action_type": {
                    "description": "the action the device is to run, e.g. `xprice/reprice-summary`",
                    "type": "string"
                  },
                  "idempotency_key": {
                    "description": "the same (project, key) returns the existing task",
                    "type": "string"
                  },
                  "max_attempts": {
                    "description": "attempts before the task dead-letters",
                    "type": "integer"
                  },
                  "payload": {
                    "description": "parameters for the action — never prompts or credentials",
                    "type": "object"
                  },
                  "project_id": {
                    "description": "admin/dev only; a project key forces its own project",
                    "type": "string"
                  },
                  "retry_interval_secs": {
                    "description": "wait between attempts",
                    "type": "integer"
                  },
                  "source": {
                    "description": "who enqueued it",
                    "type": "string"
                  }
                },
                "required": [
                  "action_type"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "The created RelayTask, flattened, plus `admission: {queued: {eligible_devices}}` and an optional `warning` when a soft usage limit is close. A 422 `relay_unroutable` means nothing in the fleet advertises that action type.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Enqueue a device task; routability and the project's usage limits are checked first."
      }
    },
    "/v1/relay/tasks/{id}": {
      "get": {
        "description": "One relay task by id — the shape the originating app polls.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_relay_task",
        "parameters": [
          {
            "description": "relay task id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "relay task id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RelayTask"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One relay task by id — the shape the originating app polls."
      }
    },
    "/v1/relay/tasks/{id}/cancel": {
      "post": {
        "description": "Stop a queued or leased task; a finished one is a 409, never a silent success.\n\nAn admin, or a project key carrying the `manage` capability.",
        "operationId": "cancel_relay_task",
        "parameters": [
          {
            "description": "relay task id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "relay task id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RelayCancel"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Stop a queued or leased task; a finished one is a 409, never a silent success."
      }
    },
    "/v1/relay/tasks/{id}/progress": {
      "post": {
        "description": "Record what a leased run is doing; fenced exactly like renew.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "post_relay_progress",
        "parameters": [
          {
            "description": "relay task id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "relay task id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "fence": {
                    "description": "the `lease_fence` handed out at lease time",
                    "type": "string"
                  },
                  "progress": {
                    "description": "what the run is doing now",
                    "type": "string"
                  }
                },
                "required": [
                  "fence",
                  "progress"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeaseHeld"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record what a leased run is doing; fenced exactly like renew."
      }
    },
    "/v1/relay/tasks/{id}/renew": {
      "post": {
        "description": "Extend a held lease; a 409 means the lease is gone and the device must stop.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "renew_relay_lease",
        "parameters": [
          {
            "description": "relay task id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "relay task id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "fence": {
                    "description": "the `lease_fence` handed out at lease time",
                    "type": "string"
                  }
                },
                "required": [
                  "fence"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeaseHeld"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Extend a held lease; a 409 means the lease is gone and the device must stop."
      }
    },
    "/v1/relay/tasks/{id}/result": {
      "post": {
        "description": "A device reports a run's outcome; a landed report also writes the run's usage event.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "post_relay_result",
        "parameters": [
          {
            "description": "relay task id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "relay task id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "action_version": {
                    "description": "the version the action declares",
                    "type": "string"
                  },
                  "cost_usd": {
                    "description": "what the envelope says it cost — the book and flat rate are fallbacks",
                    "type": "number"
                  },
                  "error": {
                    "description": "what went wrong, for `failed` / `deferred`",
                    "type": "string"
                  },
                  "fence": {
                    "description": "the `lease_fence` handed out at lease time; a stale one is a 409",
                    "type": "string"
                  },
                  "input": {
                    "description": "the rendered prompt, only when the action set `report_io`",
                    "type": "object"
                  },
                  "input_tokens": {
                    "description": "usage from the CLI envelope",
                    "type": "integer"
                  },
                  "latency_ms": {
                    "description": "how long the run took",
                    "type": "integer"
                  },
                  "mode": {
                    "description": "the posture it ran under (generate | readonly-scan | edit)",
                    "type": "string"
                  },
                  "model": {
                    "description": "model the run used",
                    "type": "string"
                  },
                  "output": {
                    "description": "the result text, only when the action set `report_io`",
                    "type": "object"
                  },
                  "output_tokens": {
                    "description": "usage from the CLI envelope",
                    "type": "integer"
                  },
                  "prompt_sha256": {
                    "description": "fingerprint of the rendered prompt actually executed",
                    "type": "string"
                  },
                  "result": {
                    "description": "the action's result, for `succeeded`",
                    "type": "object"
                  },
                  "retry_after_secs": {
                    "description": "for `deferred`: when to try again",
                    "type": "integer"
                  },
                  "status": {
                    "description": "the run's outcome",
                    "enum": [
                      "succeeded",
                      "failed",
                      "deferred"
                    ],
                    "type": "string"
                  }
                },
                "required": [
                  "status"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RelayTask"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "A device reports a run's outcome; a landed report also writes the run's usage event."
      }
    },
    "/v1/revenue": {
      "post": {
        "description": "Record one revenue event (manual post, or a billing sync) for profit tracking.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "post_revenue",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevenueEvent"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevenueEvent"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record one revenue event (manual post, or a billing sync) for profit tracking."
      }
    },
    "/v1/revenue/reprice": {
      "post": {
        "description": "Restate revenue stored at the 1:1 FX fallback once a real rate exists; previews by default.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "reprice_revenue",
        "parameters": [
          {
            "description": "ISO-4217 code to restate, e.g. `GBP`",
            "in": "query",
            "name": "currency",
            "required": true,
            "schema": {
              "description": "ISO-4217 code to restate, e.g. `GBP`",
              "type": "string"
            }
          },
          {
            "description": "scope to one project; absent restates every project",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "scope to one project; absent restates every project",
              "type": "string"
            }
          },
          {
            "description": "USD per major unit; absent takes the server's current FX book rate",
            "in": "query",
            "name": "rate",
            "required": false,
            "schema": {
              "description": "USD per major unit; absent takes the server's current FX book rate",
              "type": "number"
            }
          },
          {
            "description": "`1`/`true` (the DEFAULT) previews and writes nothing; `0` applies the restatement",
            "in": "query",
            "name": "dry_run",
            "required": false,
            "schema": {
              "description": "`1`/`true` (the DEFAULT) previews and writes nothing; `0` applies the restatement",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ currency, rate, book_version, matched, changed, dry_run } — `matched` counts the 1:1-fallback rows in that currency, `changed` the subset that carried a minor-unit figure to re-multiply.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Restate revenue stored at the 1:1 FX fallback once a real rate exists; previews by default."
      }
    },
    "/v1/rollup": {
      "get": {
        "description": "The grouped cost/usage primitive every fixed cost surface is one grouping of.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_rollup",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "comma-separated dimensions, 1..=3 (default provider,model): project|provider|model|name|api_key|customer|product|prompt|day",
            "in": "query",
            "name": "by",
            "required": false,
            "schema": {
              "description": "comma-separated dimensions, 1..=3 (default provider,model): project|provider|model|name|api_key|customer|product|prompt|day",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (default 30d ago)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (default 30d ago)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window end (exclusive)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 window end (exclusive)",
              "type": "string"
            }
          },
          {
            "description": "which timestamp the window and `day` bucket read (default ts; accounting reads use received_at)",
            "in": "query",
            "name": "time",
            "required": false,
            "schema": {
              "description": "which timestamp the window and `day` bucket read (default ts; accounting reads use received_at)",
              "enum": [
                "ts",
                "received_at"
              ],
              "type": "string"
            }
          },
          {
            "description": "comma-separated `dimension:value` equality predicates, e.g. customer:acme,model:gpt-5.4",
            "in": "query",
            "name": "filter",
            "required": false,
            "schema": {
              "description": "comma-separated `dimension:value` equality predicates, e.g. customer:acme,model:gpt-5.4",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ group_by: [dimension], time_key, rows: [{keys, calls, tokens, cost_usd, unpriced_calls, …}] } — the grouping is echoed so `keys` reads positionally.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The grouped cost/usage primitive every fixed cost surface is one grouping of."
      }
    },
    "/v1/rubrics/{id}": {
      "get": {
        "description": "One rubric, plus whether any judge has been calibrated against it.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_rubric",
        "parameters": [
          {
            "description": "rubric id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "rubric id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "A `Rubric` flattened, plus `active` (has anything ever been calibrated against this id) and `calibrated_judges` — a new version inherits neither.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One rubric, plus whether any judge has been calibrated against it."
      }
    },
    "/v1/rubrics/{id}/versions": {
      "post": {
        "description": "Mint the next generation of a rubric as a new row, so stored verdicts keep their meaning.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "create_rubric_version",
        "parameters": [
          {
            "description": "the rubric to supersede",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "the rubric to supersede",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "dimensions": {
                    "description": "weighted scoring dimensions the judge fills per case",
                    "items": {
                      "properties": {
                        "anchors": {
                          "description": "anchored level descriptions, e.g. [\"1.0 = fully correct\", \"0.5 = minor error\", \"0 = wrong\"]",
                          "items": {
                            "type": "string"
                          },
                          "type": "array"
                        },
                        "description": {
                          "description": "what this dimension measures",
                          "type": "string"
                        },
                        "floor": {
                          "description": "gating floor: if this dimension scores below it, the case fails regardless of the overall",
                          "type": "number"
                        },
                        "key": {
                          "description": "stable identifier used in the judge's JSON output (e.g. correctness)",
                          "type": "string"
                        },
                        "weight": {
                          "description": "relative weight in the overall score (default 1.0)",
                          "type": "number"
                        }
                      },
                      "required": [
                        "key",
                        "description"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "threshold": {
                    "description": "the new pass threshold; omitted ⇒ carried forward",
                    "type": "number"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Rubric"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Mint the next generation of a rubric as a new row, so stored verdicts keep their meaning."
      }
    },
    "/v1/schedules": {
      "get": {
        "description": "Every recurring workload in this deployment, across projects.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "list_all_schedules",
        "parameters": [
          {
            "description": "one project's schedules; omit for every project's",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "description": "one project's schedules; omit for every project's",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Schedule"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Every recurring workload in this deployment, across projects."
      }
    },
    "/v1/schedules/{id}": {
      "delete": {
        "description": "Remove a schedule; the history of what it enqueued survives it.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "delete_schedule",
        "parameters": [
          {
            "description": "schedule id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "schedule id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ deleted: id } — the jobs it already produced are kept.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Remove a schedule; the history of what it enqueued survives it."
      },
      "put": {
        "description": "Patch a schedule; an omitted field is left alone, so pausing cannot rewrite a payload.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "update_schedule",
        "parameters": [
          {
            "description": "schedule id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "schedule id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "enabled": {
                    "description": "pause (false) or resume (true) it",
                    "type": "boolean"
                  },
                  "interval_secs": {
                    "description": "change how often it fires (floor 60)",
                    "type": "integer"
                  },
                  "payload": {
                    "description": "replace the payload",
                    "type": "object"
                  },
                  "type": {
                    "description": "change the job kind it enqueues",
                    "enum": [
                      "bench_run",
                      "score_events",
                      "score_traces",
                      "dataset_sample",
                      "calibrate"
                    ],
                    "type": "string"
                  }
                },
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Schedule"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Patch a schedule; an omitted field is left alone, so pausing cannot rewrite a payload."
      }
    },
    "/v1/schedules/{id}/runs": {
      "get": {
        "description": "The jobs one schedule has produced, matched by the `schedule_id` its sweep stamps.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "schedule_runs",
        "parameters": [
          {
            "description": "schedule id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "schedule id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Job"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "The jobs one schedule has produced, matched by the `schedule_id` its sweep stamps."
      }
    },
    "/v1/scores": {
      "get": {
        "description": "Judge verdicts, newest first; narrowable to one run, rubric, kind, or the review queue.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "list_scores",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "only this benchmark run's case results, in case order",
            "in": "query",
            "name": "run",
            "required": false,
            "schema": {
              "description": "only this benchmark run's case results, in case order",
              "type": "string"
            }
          },
          {
            "description": "only verdicts judged against this stored rubric; survives a rubric rename, unlike the free-text `rubric` label",
            "in": "query",
            "name": "rubric_id",
            "required": false,
            "schema": {
              "description": "only verdicts judged against this stored rubric; survives a rubric rename, unlike the free-text `rubric` label",
              "type": "string"
            }
          },
          {
            "description": "only verdicts of this kind",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "description": "only verdicts of this kind",
              "enum": [
                "freeform",
                "rubric",
                "bench_case",
                "compare_cell",
                "pairwise_game",
                "calibration",
                "trace"
              ],
              "type": "string"
            }
          },
          {
            "description": "max scores (default 50, max 1000; run-scoped: default 5000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max scores (default 50, max 1000; run-scoped: default 5000)",
              "type": "integer"
            }
          },
          {
            "description": "1/true keeps only verdicts a human should look at (M11)",
            "in": "query",
            "name": "needs_review",
            "required": false,
            "schema": {
              "description": "1/true keeps only verdicts a human should look at (M11)",
              "type": "string"
            }
          },
          {
            "description": "the pass threshold `needs_review` measures near-misses against (default 0.7)",
            "in": "query",
            "name": "threshold",
            "required": false,
            "schema": {
              "description": "the pass threshold `needs_review` measures near-misses against (default 0.7)",
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Score"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Judge verdicts, newest first; narrowable to one run, rubric, kind, or the review queue."
      },
      "post": {
        "description": "Record one judge verdict; the rolling quality-regression window sees it.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "post_score",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Score"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Score"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Record one judge verdict; the rolling quality-regression window sees it."
      }
    },
    "/v1/storage/status": {
      "get": {
        "description": "Disk accounting per table and index, per-family latency, and the maintenance ledger.\n\nAdmin (or dev-mode) principals only.",
        "operationId": "get_storage_status",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ tables: [{name, bytes, rows}], indexes: […], latency: {family: …}, maintenance: [{at, action, deferred_because?}] } — disk accounting plus the maintenance flight recorder, including the passes that were DEFERRED.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Disk accounting per table and index, per-family latency, and the maintenance ledger."
      }
    },
    "/v1/traces": {
      "get": {
        "description": "Traces (events grouped by trace_id), newest first.\n\nAn admin, or a project key carrying the `read` capability.\n\nPaged: the next page's keyset cursor comes back in the `X-Next-Cursor` response header, or as a `next_cursor` field of the body where the handler wraps its rows. Pass it back as `cursor`.",
        "operationId": "list_traces",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "max traces (default 20, max 1000)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "description": "max traces (default 20, max 1000)",
              "type": "integer"
            }
          },
          {
            "description": "RFC3339 lower bound on the trace's end time (inclusive)",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 lower bound on the trace's end time (inclusive)",
              "type": "string"
            }
          },
          {
            "description": "RFC3339 upper bound on the trace's end time (exclusive)",
            "in": "query",
            "name": "until",
            "required": false,
            "schema": {
              "description": "RFC3339 upper bound on the trace's end time (exclusive)",
              "type": "string"
            }
          },
          {
            "description": "keep only traces of this status",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "description": "keep only traces of this status",
              "enum": [
                "success",
                "error"
              ],
              "type": "string"
            }
          },
          {
            "description": "minimum whole-trace cost (USD)",
            "in": "query",
            "name": "min_cost",
            "required": false,
            "schema": {
              "description": "minimum whole-trace cost (USD)",
              "type": "number"
            }
          },
          {
            "description": "keyset cursor from a prior call's next_cursor",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "description": "keyset cursor from a prior call's next_cursor",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[{ trace_id, project_id, started_at, ended_at, spans, cost_usd, tokens, status }] — one rollup row per trace.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Traces (events grouped by trace_id), newest first."
      },
      "post": {
        "description": "OTLP/HTTP JSON export door: GenAI spans fan into the native batch write path.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "post_traces_otlp",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "additionalProperties": true,
                "description": "An OTLP/HTTP JSON `ExportTraceServiceRequest`; OTel GenAI spans become events.",
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ accepted, rejected, invalid, results: […] } — as the batch door.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "OTLP/HTTP JSON export door: GenAI spans fan into the native batch write path."
      }
    },
    "/v1/traces/{id}": {
      "get": {
        "description": "One trace: totals, span tree, and scores within it.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_trace",
        "parameters": [
          {
            "description": "trace id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "trace id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "{ trace: {…totals}, spans: [ …tree… ], scores: [Score] } — one trace's rollup, its span tree, and the verdicts recorded within it.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "One trace: totals, span tree, and scores within it."
      }
    },
    "/v1/traces/{id}/score": {
      "post": {
        "description": "Score a whole trace, anchored to its root span.\n\nAn admin, or a project key carrying the `ingest` capability.",
        "operationId": "score_trace",
        "parameters": [
          {
            "description": "trace id — the score is anchored to its root span",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "description": "trace id — the score is anchored to its root span",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "properties": {
                  "cost_usd": {
                    "description": "what judging it cost",
                    "type": "number"
                  },
                  "event_id": {
                    "description": "anchor the verdict to one span inside the trace instead of its root",
                    "type": "string"
                  },
                  "max": {
                    "description": "the scale's maximum (default 1)",
                    "type": "number"
                  },
                  "pass": {
                    "description": "did it pass",
                    "type": "boolean"
                  },
                  "reasoning": {
                    "description": "why",
                    "type": "string"
                  },
                  "rubric": {
                    "description": "the rubric label this verdict was judged against",
                    "type": "string"
                  },
                  "scored_by": {
                    "description": "who or what scored it (default `mcp`)",
                    "type": "string"
                  },
                  "value": {
                    "description": "the score",
                    "type": "number"
                  }
                },
                "required": [
                  "rubric",
                  "value"
                ],
                "type": "object"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Score"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Score a whole trace, anchored to its root span."
      }
    },
    "/v1/usecases": {
      "get": {
        "description": "Usage and cost grouped by use-case name, provider and model.\n\nAn admin, or a project key carrying the `read` capability.",
        "operationId": "get_usecases",
        "parameters": [
          {
            "description": "",
            "in": "query",
            "name": "project",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "RFC3339 window start (inclusive); omit for full history",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "description": "RFC3339 window start (inclusive); omit for full history",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "description": "[{ name, provider, model, calls, input_tokens, output_tokens, cost_usd, unpriced_calls }] — usage and cost per use-case; `name` is null for unnamed calls.",
                  "type": "object"
                }
              }
            },
            "description": "Success."
          },
          "401": {
            "description": "Missing or invalid credential."
          },
          "403": {
            "description": "The principal lacks the capability this operation declares."
          },
          "501": {
            "description": "This deployment's store backend does not implement the surface. A permanent gap on this backend, never 'you have no data' — see GET /v1/capabilities."
          }
        },
        "summary": "Usage and cost grouped by use-case name, provider and model."
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
